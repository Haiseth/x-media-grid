# X Media Grid Restore

x.com の見た目だけを書き換えるBrave/Chrome拡張機能（表示専用。自動いいね・自動フォロー・投稿・データ送信は一切しない）。

## 導入方法（Brave）

1. `brave://extensions` を開く
2. 右上の「デベロッパーモード」をON
3. 「パッケージ化されていない拡張機能を読み込む」→ このフォルダ（`x-media-restore`）を選択
   （既に読み込み済みなら、このアイコンの「再読み込み」ボタンを押せばOK）
4. x.com を開き直す（既に開いていたタブはリロード）

## v3.74.0：戻る操作の復元を2.1倍高速化・更新処理の永久フリーズ対策・tabs権限を廃止

### 戻り復元 1414ms → 660ms（実機計測）
遅延の原因は両方ともX側ではなく自前のコードだった。

- **`waitFor()`が条件を同期的に確認していなかった**。必ずsetIntervalの1周期目を
  待つ作りだったため、条件が既に満たされていても待たされる。しかもXの再描画で
  メインスレッドが忙しいと、50ms指定の周期が**実測449ms**まで伸びていた。
  タイマーを仕掛ける前に1回確認して即座に返すようにした。
- **同じ条件を2回待っていた**。数行前で完了しているタイムラインセルの待ちを、
  キャッシュ復元経路でもう一度待ち直しており、この重複だけで**実測357ms**。
  掴んだ要素を使い回し、Xがその間に作り直していた時だけ待ち直す。

計測ログで確認：待ち時間の合計が994ms→5msに縮小。

### 更新処理の永久フリーズ対策
`refreshHomeTimeline()`をtry/finallyで囲んだ。この関数はXのDOMを大量に触り
（detachされ得る要素のclick、getComputedStyleでの祖先walk等）、しかも4箇所から
fire-and-forgetで呼ばれるため例外を誰も捕まえない。途中で例外が出ると
`Grid.holdTop`と`Grid.refreshing`が立ったままになり、**pumpMoreが二度と動かず
（追加読み込みが永久停止）・更新ボタンが二度と効かず・「更新中…」が出っぱなし・
Xの本物のヘッダーがグリッドの上に出たまま**になる。通常経路では従来どおり
resetGridEntriesの前にフラグを解除して順序を保ち、finallyは例外時の保険とした。

### tabs権限を廃止（要求する権限は storage のみに）
`tabs`を使っていたのはbackground.jsの「拡張から開いたタブを閉じたら元のタブへ
フォーカスを戻す」処理だけだったが、(1) v3.55.0でR/Fが同タブSPA遷移になり
**拡張機能自身は新しいタブを開かなくなっていた**、(2) MV3のservice workerは
アイドルで停止するため、モジュールスコープのopenerMapが消えて**機能自体
ほとんど働いていなかった**。この機能のためにブラウザ全体のタブ生成を監視し
開いた側のURLを読む権限を要求するのは割に合わないため、処理ごと削除した。
PRIVACY.md / README.md の記述も実態に合わせて更新。

### その他
- `content_scripts.matches` を `*://` から `https://` に限定。
- 過去のバージョン更新コマンドが混入させていた**UTF-8 BOMをmanifest.jsonから除去**。
- `currentHomeTabText()`のフォールバックが日本語リテラル`'おすすめ'`だったため、
  英語UIで「Images only (おすすめ)」と混ざり得た。実際の先頭タブ名を使い、
  それも無ければ括弧ごと省く（新キー`imageOnlyLabelPlain`を4テーブル全てに追加）。

## v3.73.0：スクロールして見ている最中に勝手に左上へ戻されるバグを修正

実機報告：Fでメディア欄を開いてスクロールしていたら、時々一番左上に戻された。

原因：初回の一括読み込み(pumpMore)が完了した時のコールバックが、並び順を
整えた後に選択位置を0へリセットし、paintSelection()がそのタイルへスクロール
していた。この処理を抑止するフラグ(userHasNavigated)はWASD/Spaceでしか
立たないため、**マウスホイールだけで見ている人は「まだ操作していない」扱いの
まま**で、読み込みが終わった瞬間に位置を奪われていた。読み込み完了の
タイミング次第なので「たまにある」という症状になる。

修正：`Grid.userHasScrolled`を新設し、自前のscrollIntoView由来ではない
（＝本人が動かした）スクロールで立てる。立っている場合は
`settleGridOrderKeepingView()`を使い、並び順は整えつつ、選択中のタイルを
同じ投稿に指し直し、画面内の基準タイルを使ってスクロール位置を補正して
**見ている場所を動かさない**。

## v3.72.0：更新の所要時間を短縮（3〜6秒→約2.4秒）

実機フィードバック「更新は正しく動くようになったが3〜6秒は少し長い」への対応。
固定待ちを、条件が満たされ次第すぐ進むポーリングに置き換えた。

- 「上端に着くまで」「バーが描画されるまで」「バーが消えるまで」を全て
  waitFor()化。
- **新着シグナル（未消化の告知ピル／青ドット）が最初から無い時は、取り込み
  バーを探す待ちを丸ごと飛ばす**（バーは絶対に出ないため）。これだけで
  新着なしの更新が約1.9秒短縮。
- 仕上がり判定のループを300ms刻み→150ms刻みにし、先頭が入れ替わった瞬間に抜ける。
- 上端に留まるループは青ドットを80ms毎に見て、消えた瞬間に抜ける。最初から
  未読が無ければほぼ即座に抜ける。

実機計測：新着なしの更新を連続2回で2.5秒・2.4秒（従来3.0秒）、バッジは消えたまま。

## v3.71.0：青バッジが消えない真因を特定（Xは「一番上を実際に見た」ことで未読を解除していた）

**実機報告が決定打**：「ONでホームを押してもバッジが取れない。OFFにして上まで
見に行ったら『56件』みたいなボタンは無く、**上まで行った時点でバッジが取れた**」。
つまりXは、何かのボタンが押されたことではなく、**タイムラインの一番上が実際に
見えている状態**を観測して未読状態（青ドット・新着の告知）を解除している。

**なぜグリッドONだと解除されなかったか**：
- 更新処理の最後で`resetGridEntries()`→`pumpMore()`が走り、タイルを集めるために
  ウィンドウを1秒足らずで2万px（実測では最大8万px）下へ動かしていた。上端に
  居た時間が実質ゼロなので、Xが「上にいる」と認識する前に離脱していた。
- さらにXのヘッダー（タブ一覧＝告知が住む領域）をこちらが`display:none`で
  隠していた。display:noneはレイアウトごと消すため、その中の可視判定
  （IntersectionObserver等）が一切動かない。

**修正**：
- `Grid.holdTop`フラグを新設。立っている間は`pumpMore()`が開始せず、進行中の
  ものもループを抜けるので、タイル収集がウィンドウを引きずり下ろせない。
- 更新の合流後、`scrollY`を0に固定したまま最大3秒留まる（ドットが実際に
  消えたら即座に抜けるので無駄に待たない）。
- その留まっている間だけXのヘッダーの`display:none`を外し、X自身の可視判定が
  動けるようにする（グリッド本体が上に被さっているのでユーザーには見えない）。
- 上端に留まっている間は「取り込みバーの有無」が未読残存の確定情報になるので、
  無ければピルに消化済みの印を付けてバナーを確実に消す。
- **バナーの点灯条件から青ドットを除外**。ドットは「一番上を実際に見る」まで
  消えないX側の仕様なので、条件に入れているとグリッド表示中は永久点灯になり、
  これが「消えた瞬間がない」の直接原因だった。90秒スヌーズという誤魔化しも削除。

## v3.70.0：自動リロードを全廃

実機フィードバック「リロードするうざい、消して、今試してるのにそれが台無しに
なる」を受け、ページを自動リロードする経路を3つとも撤去した。

- `tryResyncReload()`は常にfalseを返す（＝何もしない）ようにした。呼び出し元は
  2箇所（更新の待機ループ、グリッド初回起動）とも、もともとリロードしない
  場合のフォールバック（scrollToのリトライ継続、「マウスホイールを一度
  スクロールしてください」の案内）を持っているので、動作は止まらない。
- メディア/いいね/ブックマークの更新ボタンは、新着取り込みバーが無いため
  リロードで作り直していた。グリッドだけを組み直す方式(resetGridEntries)に変更。
- Fキーでメディア欄を開いた時に1回だけ自動リロードするための`#xmr-fresh`
  マーカーの付与をやめた（同タブSPA遷移が主流になり不要になっていた）。

## v3.69.0：新着取り込みの本体（「◯件のポストを表示」バー）を発見して採用・更新14秒→4.4秒

**実機報告からの発見**：「一番上までスクロールすると『56件を表示』みたいな
四角いボタンが出てくる」。これまで追いかけていた青い楕円のピル
（「〇〇さんがポストしました」）とは**完全に別の要素**で、こちらが新着取り込みの
本体だった。タイムラインの**先頭セル**に現れる`<button>`で、ウィンドウが
最上部にある時だけ仮想リストに描画される。

- **ピルと違い、このバーは合成クリックが普通に効く**（実機確認：押すと
  バーが消え、先頭が9時間前の投稿から1時間前の投稿へ入れ替わった）。
  v3.63〜3.67で積み上げた「新着の合流にはトラステッドクリックが必須」という
  結論は、**そもそも間違った要素を押していたことによる誤りだった**。
- 更新処理を「最上部へ戻す→バーを探す→押す」に変更。バーが無い時だけ
  従来のホームリンク経由の引き直しにフォールバックする。
  検出は言語非依存の構造判定（先頭cellInnerDiv・articleを含まない・短い
  テキストのボタンを持つ）。
- **待機ループの空回りバグを修正**：先頭セルを「必ずツイート」と決め打ちして
  いたため、取り込みバーが先頭に居座ると毎回スキップし、40回×300ms＝12秒
  空転して「更新中…」が終わらなかった。ツイートを持つ最初のセルを探す形に
  修正。あわせて指紋の採取を「最上部へ戻した後」に変更（深い位置で採ると
  上へ戻しただけで別ツイートになり誤判定していた）。
  **実機計測：更新完了まで14秒→4.4秒**。
- **バナーの点灯条件を見直し**（実機報告「消えた瞬間がないわけ」）：青ドットは
  取り込みバーで29件を実際に合流させても**一度も消えない**ことを実測
  （0.5秒刻み×20回すべて点灯）。新着が絶え間なく届くフィードではドットも
  ピルも数秒で復活するため、そのまま条件にすると永久点灯になる。更新完了後
  90秒はバナーを黙らせ、「取り込んだ」ことが目で分かるようにした。

## v3.68.0：ホーム更新が効かなかった真因を特定・修正（クリックとスクロールの順序が逆だった）

**真因**：Xのホームボタンは2段構えの挙動をする。
「下にスクロールしている時＝一番上へ戻るだけ」「既に一番上にいる時＝新着を
取り込む」。グリッド表示中はタイルを集めるためにウィンドウを深く
（実測17000〜32000px）スクロールしたままなので、**ホームボタンを押しても
1発目が「上へ戻る」に消費され、取り込みまで永久に進まなかった**。
従来のコードは「クリック→その後にscrollTo(0,0)」という逆順だったため、
自前の更新ボタンも、ユーザー自身のホームクリックも、毎回この罠を踏んでいた。
これが以下の全症状の共通原因だった：

- 画像のみ表示OFFならホームボタンで新着が入るのに、ONだと入らない
- 更新しても同じ投稿が並び、既読の色だけが付く
- 新着ボタン（「〇〇さんがポストしました」）が押しても消えない

**修正**：更新処理を「先に一番上へ戻す→Xが『もう上にいる』と認識するまで
待つ→それからホームリンクをクリック」の順序に変更。ユーザー自身がホーム
ボタンを押した時も同じ手順を走らせる（1発目が上へ戻るのに使われるため、
こちらで改めて取り込みまで運ぶ）。

**実機検証**：新着ボタン点灯・ウィンドウ31962px深くの本番同条件で、ホーム
ボタンのトラステッドクリック→旧・新着ボタンが消化された（中身の指紋が
入れ替わったことで確認）→グリッドが新しい内容で再構築→リロードなし。

なお、この検証で「拡張からの合成クリックでも、先に上へ戻してからなら
新着が取り込まれる」ことも確認できた。v3.63〜3.67で「ピルは合成クリック
不能」「trusted必須」と結論していたのは誤りで、実際にはスクロール位置の
問題だった（新着ボタン自体を押す必要はなく、ホームボタン経由で取り込める）。

- **受け渡し方式を撤去**（実機フィードバック「それ配布するの嫌だな」）。
  画面が勝手に切り替わる挙動は無くなり、バナーは押せば普通に更新する。

## v3.67.0：リロード必須を撤回・ピル受け渡し方式へ・拡大表示のW/Sで裏の一覧も追従

- **リロード必須を撤回**（実機フィードバック「ページのリロードを必ずするのは
  もう最悪、ストレス最大」）。更新は再びホームリンク経由の引き直し（先頭
  ツイートの入れ替わりを待つレース対策込み）に戻し、リロードは仮想リストが
  停止した時の最後の保険(tryResyncReload)だけに限定。
- **新着ピルは「受け渡し」方式に**：表示状態の本物ピルがある時にバナーを
  押すと、グリッドを一時解除してピルをそのまま見せ、ユーザーのトラステッド
  クリックに委ねる。押されたら合流完了（ピルの格納）を待って自動でグリッド
  再開＋新着入りで組み直し。45秒放置でグリッド復帰。v3.66でこの方式を
  「ピルが格納状態から出てこない」として棄却したが、格納状態(aria-hidden)を
  検出から除外できるようになったため、受け渡し発動時は必ずピルが見えている。
- **「裏でピル実行」の再検証と最終結論**：v3.65までの合成クリック実験は
  格納状態のピルに対してだった可能性があったため、表示状態（aria-hidden無し・
  opacity:1）のピルで全て再検証した。結果は同じく全滅：合成click、
  MouseEventフルシーケンス、isTrusted偽装、**Reactのprops.onClick直呼び**
  （ハンドラの存在は確認・例外なしで実行されるが合流は起きない）、SPA往復、
  最上部での待機。Xは信頼済みポインタ入力でしか合流しない。よってユーザー
  自身のクリックを介す受け渡し方式が唯一の正解と確定。
- **拡大表示のW/Sで裏の一覧が追従しないバグを修正**（実機報告）：拡大表示の
  ままSで下の投稿へ送っても、裏のグリッドのスクロール位置が置き去りになり、
  閉じた後にマウスで下までスクロールし直す必要があった。paintSelection()の
  スクロール追従がGrid.level==='grid'限定だったのが原因。ビューア中も追従
  させ、さらにビューアのA/D・W/S送りで裏の選択インデックスも同期するように
  した（実機確認：Sを6回で裏が446→835→…→2361pxと追従し、閉じた直後に
  選択タイルが画面内）。
- 複数画像一覧(sub)でもW/S移動時に選択画像へスクロール追従するよう追加。

## v3.66.0：ホーム更新は「リロード一択」に確定・格納状態ピルの識別（実機報告「使用感悪すぎる」への最終回答）

- **実機報告**：更新してもピルのポスト群が入ってこない／同じ内容が既読付きで
  並び直すだけ／バナーが消えない。手動の「画像のみ表示OFF→本物ピルを
  クリック→ON」でだけ本当に更新される。
- **原因の核心**：ホームリンク合成クリックによる更新は「おすすめの引き直し」
  であって、ピルが告知するポスト群の合流ではない。合流を起こせるのは本物
  ピルへのトラステッドクリックのみで、これは拡張からは合成不可能（v3.63〜65で
  click/PointerEvent列/キーボードショートカット'.'まで全て実機で無効を確認）。
- **さらに「ピル受け渡し」案も実機で棄却**：バナークリックでグリッドを
  一時解除し本物ピルをユーザーに直接クリックさせる方式を実装したが、
  Xはピルを**格納状態**（aria-hidden="true"・ラッパーopacity:0・アバター0個）
  でDOMに保持したまま、トラステッドの上スクロールでも表示状態に出さない
  ケースがあり、「見えないボタンは押せない」ため不成立。
- **最終解**：ホームの更新（更新ボタン/cキー/新着バナー/ホームアイコン）は
  **ページリロード一択**。常に最新TLが先頭から得られ、ピル/ドットの状態も
  Xが正しく作り直す。既読はflushしてから移るので失われず、新着は未読のまま
  入る（「未読のみ表示」運用もそのまま）。所要2〜4秒はUXコストだが、
  「押したのに変わらない」を構造的に排除した。
- **格納状態ピルの識別**（上記の副産物・実機で確定したシグネチャ）：
  aria-hidden="true"のピルは新着シグナルとして扱わない。これでリロード直後や
  ゾンビ由来のバナー誤点灯が消えた。ドット起因の再点灯も更新後3分間
  スヌーズ（sessionStorageでリロードをまたいで有効）。

## v3.65.0：ピル「穴あき表示」実験と撤退・ゾンビ判定の指紋化

- **実験と撤退の記録**：合成クリック不能なピルに「ユーザーの本物クリックを
  直接届かせる」ため、(a)ピルを内包する隠しコンテナだけvisibility:hidden化
  してピルをvisible化 (b)Reactのインラインstyleリセットに勝つための
  CSS変数+!importantによる位置調整 (c)シェルへのclip-path穴あけ、まで
  実装したが、**Chromiumはclip-pathの穴（evenodd/nonzero逆回り内周とも）を
  「描画」にしか適用せず、子孫要素のヒットテストには適用しない**ことが
  実機で確定し撤退。ピルはz0/z1スタッキングコンテキストに多重に閉じ込め
  られておりz-indexでも持ち上げ不能。結論：拡張からピルへユーザークリックを
  届ける手段は存在しない。更新はホームリンク合成クリック＋失敗時リロードで
  行う（v3.64方式に一本化）。経緯はcontent.cssの供養コメントにも記録。
- **ゾンビ判定を指紋方式に強化**：消化済みマークを単なるフラグから
  「ピル内の投稿者アバターURL群の指紋」に変更。Xが同じDOM要素を使い回して
  次の新着を告知した場合でも、指紋が変われば新しい告知として拾い直せる。
- **E2E実機確認**：バナークリック→TL先頭入れ替わり→グリッド再構築（タイル
  数変化）→ゾンビ消化→バナー消灯まで、リロード無しのファストパスで成功。

## v3.64.0：ホーム更新の根本修正（実機ライブ検証で3つの真因を特定）

実機報告「更新ボタンを押しても同じ内容」「新着バナーを押しても読み込まれない」
を、出ているピルを使ったライブ検証で解体。判明した真因は3つ：

- **真因1（ピル判定の自滅）**：本物の新着ピルはXのタブヘッダー内にあり、
  グリッド表示中はそれを自前の`.xmr-tablist-hide`(display:none)で隠して
  いるため、v3.62の「矩形サイズ>0＝表示中」判定が本物まで弾いていた。
  → 判定を「display:none祖先の有無（ただし自前クラスによるものはセーフ）」
  に変更。
- **真因2（ピルは合成クリック不能）**：ピルのボタンは、隠し解除して表示
  状態(284×40)にしてもclick()にもPointerEventフルシーケンスにも一切反応
  しない（trusted要求と推定。実機確定）。一方**ホームナビリンクへの合成
  click()は確実に効く**（TL全体が最新ミックスに入れ替わることを確認）。
  → 更新経路をホームリンク一本に統一、ピルのクリックは全廃。
- **真因3（レース）**：ホームクリック自体は効いていたのに、Xが新TLを
  fetchし終える前（1〜2秒かかる）に待機ループが「旧リストの先頭がty<400」
  で即抜けし、**旧内容のままグリッドを組み直していた**。これが「更新した
  のに同じ」の正体。→ 先頭ツイートの入れ替わりを待ち条件に追加（最大6秒。
  新着ゼロの場合を考慮して無限には待たない）。
- **ゾンビピルの発見と対処**：TL更新成功後もピル要素はDOMに残り続ける
  （青ドット消灯・TL入れ替わり後も残留を実機確認）。残留ピルを再検出して
  「更新できていない」ように見えるのを防ぐため、更新完了時にマークして
  以後のバナー検出から除外（Xが次の新着で出し直すピルは別要素なので拾える）。
- **最終手段**：更新前に新着シグナル（ピル/ドット）があったのに先頭が
  1件も変わらなかった場合（＝直後の再fetchをXが省略したケース。ページ
  リロード直後に実機で再現）はリロードで確実に最新化（同一URL 5分に1回まで）。
- 既知の限界：ホームの青ドットは**本物のクリックでも即座には消えない**
  ことを実機確認（Xそのものの挙動）。激流フィードでは数十秒で新着が
  再度積まれるため、バナー/ドットが点き直すのは正常動作。

## v3.62.0：グリッド上の新着バナー（実機DOM観測ベースで作り直し）・ナビバッジの表示設定

- **グリッド上に「↑ 新しいポストを表示」バナーを新設**（実機フィード
  バック：ONの時もXと同じ新着通知が欲しい＋クリックでその分を読み込み
  たい）。表示トリガーは2系統：①Xの新着ピル（MutationObserverの実機観測で
  確定した`data-testid="pillLabel"`。文言・言語・数字の有無に依存しない）
  ②ホームアイコンの青い未読ドット（戻ってきた直後などXがピルを出さない
  ケースを拾う）。クリックで本物のピル（無ければホームアイコン）経由の
  更新を実行して取り込む。設定でOFF可（初期値ON）。
- **実機で確定した罠を2つ回避**：(1)Xは非表示中のタブのタイムラインも
  DOMに保持しており、そちらのピルを掴むと「クリックしても何も起きない」
  （矩形サイズ>0の表示中ピルだけを対象にする）。(2)文言や見た目の
  ヒューリスティック検出は引用ツイートのヘッダー等を誤検出する
  （testidベースに全面置換）。
- **ナビのバッジ表示設定を追加**：ホームの青ドット非表示（初期値OFF=表示）、
  通知アイコンの数字バッジ非表示（初期値OFF=表示。ブラウザ通知で確認済みの
  人向け）。バッジ要素の構造（ナビリンク内svgの後ろのdiv）は実機DOM観測で
  確定・言語非依存。
- バナー位置をタブバー上に重なる高さへ調整（画像と被らないように）。

## v3.61.0：公開名を「Xメディアグリッド / X Media Grid」に決定・アイコン追加・新着バナーの新フォーマット対応

- **公開名を決定**：日本語環境では「Xメディアグリッド」、英語環境では
  「X Media Grid」（_localesによる言語別名称）。「グリッドは開発用語では」
  という指摘も踏まえつつ、語呂と検索性を優先。株式会社メディアグリッドは
  商標登録0件（gBizINFO確認）かつ事業非類似のためリスク低と判断。
- **アイコン追加**（16/48/128px）：黒地に2x2タイル、1枚だけ青+ハート
  （「並んだ絵の中の1枚にいいね」）。manifestにicons追記。
- **【実機報告】グリッドON時に新着（「◯◯さんがポストしました」）を
  取り込めない問題を修正**：Xが新着バナーの形式を「N件のポストを表示」
  （数字あり）から「アバター＋さんがポストしました」（数字なし）に変更して
  おり、数字必須の検出条件が空振りしていた。「アバター画像を含む・浮いて
  いる・短文のピル」という構造条件を追加（旧形式のフォールバックも残置）。
- リポジトリ公開準備：README刷新（旧READMEは本CHANGELOGに改名）、
  PRIVACY.md、LICENSE(MIT)、.gitignore（内部メモ・宣伝資料は非公開）。

## v3.60.0：初回ロード時のチラ見えも幕で隠すように・ストア公開キット作成

- **初回ロード・キャッシュ無し遷移でも幕(.xmr-veil)を張るように**（実機
  報告：キャッシュ有り時しか効いていなかった）。初回読み込みは数秒かかる
  ことがあるため最大2.5秒で自動解除する保険付き（速い通常ロードは幕の中で
  完結し、遅いケースだけ従来同様になる）。
- **store-kit/ フォルダにストア公開キット一式を作成**：名前候補・日英
  ストア掲載文・プライバシーポリシー・スクショ構成案・デモ動画絵コンテ・
  公開手順ガイド（開発者登録$5〜審査まで）。※store-kit/は提出zipに
  含めない資料。

## v3.59.0：「2回目のリプライが開けない」「いいねが外せなくなる」の根本原因を修正（sourceRoot参照の死活監視）

- **根本原因**（実機報告「1回開いて閉じてもう1回開くと『リプライ画面を
  開けませんでした』、その状態でいいねすると外せなくなる」から特定）：
  リプライの/compose往復などのタイミングでXがリスト要素ごと作り直すことが
  あり、拡張が持っている`Grid.sourceRoot`参照が「DOMから切り離された古い
  ツリー」を指したままになっていた。切り離されたツリー内でもquerySelectorは
  ヒットするため、**「ボタンは見つかるのにclick()が何も起こさない」**という
  質の悪い壊れ方になる：2回目のリプライはボタンを押せた扱いなのにモーダルが
  出ず「開けませんでした」、いいねは実際には効いていないのに拡張側の表示
  だけがON/OFFする（＝外せなく見える）。さらにobserverも死んだツリーを
  監視し続けるため追加読み込みも止まる。
- **修正**：`ensureSourceRootAlive()`を新設。sourceRootが切り離されていたら
  現在のDOMから取り直し、隠しクラスとMutationObserverを付け直す。
  articleForEntry（全アクションの入口）とharvestNew（収穫の入口）で毎回
  死活確認する。加えて、切り離されたarticle/ボタンへのclickを「成功」扱い
  しないガードをpressActionButtonOn/リポスト/リプライに追加。
- **実機検証済み**：報告と同じ再現手順（リプ開く→閉じる→もう一度開く→
  いいね→再いいね）を修正版で自動実行し、2回目のリプライ表示・いいねの
  双方向トグルとも正常を確認（実データはネットゼロに復元済み）。

## v3.58.0：リプライ関連の実機報告3件を修正

- **リプライ入力の背景に素のメディア一覧が見える**（実機報告）：再マウントの
  ためにスクロール中の裏のリストが、モーダルの背後に透けて見えていた。
  モーダル表示中はprimaryColumnごと隠すCSSで対応。
- **リプライを一度使った後、ボタンが押せなくなることがある**（実機報告）：
  多重実行防止フラグ(actionInFlight)が何らかの経路で残留すると全操作が
  永久にロックされる構造だった。8秒を超えた残留は自動解除する
  ウォッチドッグ(actionBusy)を追加（根本原因は特定しきれていないが、
  ロックが永続しないことは保証される）。
- **右サイドバー非表示をOFF→ONにした直後だけUIが崩れる**（実機報告）：
  部分的なパッチ適用(applySidebarVisibility)ではUI構成の変化（幅・検索
  ボタンの有無）を吸収しきれていなかった。hideSidebarの切替時はグリッドを
  張り替える方式に変更（キャッシュ復元があるため一瞬で、位置も保たれる）。

## v3.57.0：ホームのタブ切替バグ修正・復帰時のチラ見え対策・▶で動画再生・色カスタマイズ・デフォルト3列

- **【実機報告のバグ修正】ホームのタブ（おすすめ/フォロー中/リスト）を
  切り替えてもグリッドの中身が変わらない問題**：両タブとも画像のみ表示ON
  だとURLもmodeも変わらないため再活性化が一切走っていなかった。スコープ
  （どのタブか）の一致まで見て張り替えるように修正。スナップショットも
  タブごとのキーに分離したので、一度見たタブへの切替はキャッシュ復元で
  一瞬で戻る。
- **【実機報告】別ページからホームに戻る時に素の画面が0.4秒ほど見える件**：
  キャッシュ復元が見込める遷移では、旧グリッド解除〜新グリッド表示の間に
  背景色の幕(.xmr-veil)を張って隠すようにした（キャッシュが無い初回
  読み込みでは張らない：数秒の真っ黒画面の方が体験が悪いため）。
- **動画タイルの▶アイコンをクリックで再生へ**（実機フィードバック：
  再生ボタンを押したら再生されるのが自然）：▶クリック=そのポストへ
  同タブSPA遷移（Xの本物のプレイヤーで即再生、戻ればグリッド復帰）。
- **色カスタマイズ**（設定ページ）：アクセント色（選択枠・タブ下線・
  ボタン等の青系を一括変更）と既読の帯色を、それぞれカラーピッカーで
  変更可能に（「初期値に戻す」ボタン付き）。CSSは全面的に変数化
  (--xmr-accent-rgb / --xmr-seen-band)。
- **既読の説明を設定ページに追加**（既読の色設定の説明文を兼ねる）。
- **デフォルト列数を5→3に**（配布視点：サイドバーを隠さない標準状態なら
  3列が自然）。**列数の変更は保存されるようになった**ので、5列派は一度
  変えればそのまま（既存ユーザーは初回だけ3列になるので列数を入れ直す
  こと）。

## v3.56.0：UIをXの本来の見た目に寄せる大整理（タブバー化）・フィルタの場所別記憶・Fキーの行き先設定

- **タブ類をX本来の「下線付きタブバー」へ**（実機フィードバック：Xと
  見た目が違いすぎて初見で使いづらい）：ホームのおすすめ/フォロー中/
  リスト、アカウントのポスト/返信/リポスト/画像/動画を、ツールバー内の
  ピルから独立したタブバー(.xmr-tabbar)に移動。位置関係も本家と同じ
  （プロフィールカードの下・グリッドの上）で、アクティブタブは青い下線。
  ツールバーのすぐ下にstickyで貼り付く。「このアカウント:」ラベルは
  本家に無いので廃止。ホームのタブ切替時は下線だけがその場で追従する。
- **ツールバーは1行に**：左からいいね/ブックマーク、更新/未読のみ/
  動画のみ/画像のみ表示、右端に列数/設定/検索（検索が右上=Xの本来の
  検索位置に一致。実機フィードバックによる配置）。
- **「未読のみ表示」「動画のみ表示」を場所ごとに個別記憶**（実機
  フィードバック：全ページ共通だと使い分けできない）：画像のみ表示と
  同じscope単位（ホームのタブごと・メディア・いいね・ブックマーク・
  検索結果）。旧・共通キーからの移行つき。
- **Fキー（投稿者ページ）の行き先を設定化**：プロフィール（ポスト一覧、
  新デフォルト）とメディア欄から選択。「まずアカウントに飛びたい人の方が
  多いはず」という配布視点のフィードバックを受けてデフォルトは
  プロフィールに変更。**メディア欄派（開発者本人含む）は設定ページで
  「メディア欄」に切り替えること**。
- 実機検証はユーザー確認待ち（検証用タブがユーザー使用中の裏で待機のため）。

## v3.55.0：R/F等を同タブSPA遷移に全面変更・戻りで読み込みゼロ復元・白（ライト）テーマ対応

- **R/Fキー・ビューアのリンク・アバタークリック・ツールバーのタブピル/
  いいね/ブックマークリンクを、新しいタブ方式から「同じタブ内のSPA遷移」に
  全面変更**（実機フィードバック：新規タブはXの丸ごと初回ロードが走って
  数秒待たされ、素のXのクリック遷移に負けていた）。Xの内部ルーターに
  pushState+popstateで遷移させるため、ネイティブのクリックと同じ速さ。
  hrefは残してあるので中クリック/Ctrl+クリックで従来の新しいタブも可能。
- **戻る（ブラウザの戻る/マウスのサイドボタン）で元のグリッドへ帰ってきた
  時、読み込み直しゼロで位置ごと復元**：ページを離れる瞬間にグリッドの
  状態（エントリ・選択位置・スクロール位置・既読）をスナップショット
  (GridCache、URL毎・10分・最大8件)し、復帰時は先頭復帰もpumpMoreも
  行わずタイルを即再構築する。**副産物として動画問題も解決**：Rで
  ステータスページを開けばXの本物のプレイヤーで再生でき、戻れば
  グリッドに即復帰する。
- **白（ライト）テーマ対応**（実機フィードバック：黒前提の配色で崩壊して
  いた）：bodyの背景輝度でテーマを判定（文言非依存）し、ツールバー・
  タイルの帯・既読色・ビューアのキャプション・プロフィールカード等を
  白テーマ用配色に切り替える。画像上の半透明黒要素（バッジ・ホバー
  ボタン・トースト・拡大表示の黒背景）は両テーマ共通のまま。テーマを
  設定で切り替えた場合もページ遷移なしで追従。
- 実機検証状況：ライトテーマ判定クラスの付与は確認済み。グリッド面の
  配色・SPA遷移の速さ・戻り復元・リポスト再テストはユーザー確認待ち
  （検証用タブがユーザー使用中の裏で待機状態だったため）。

## v3.54.0：リポスト失敗の根本修正（スクロール保持のまま操作を完了）・設定ページもXの言語に追従

- **リポストが「確認ボタンが見つかりません」で失敗する不具合を修正**
  （実機報告：原神公式のポストで再現）：原因は再マウント処理の順番。
  「対象位置へスクロール→articleを取得→**元の位置へ復帰**→click」の順
  だったため、復帰した瞬間にXがセルを再アンマウントし、clickが切り離された
  要素への空振りになっていた。いいね（1クリック）はタイミング勝負でほぼ
  通っていたが、リポスト（ボタン→確認メニューの2段階）は確実に失敗する。
  共通ヘルパー`withEntryArticle()`に作り直し、**操作を最後まで実行して
  から**スクロールを復帰する順に変更（いいね/BM/リプライも同じ経路に
  統一され、全操作がより確実になった）。
- **設定ページ(options.html)もXの表示言語に追従**：content.jsが観測した
  Xの言語を共有ストレージに記録し、設定ページはja以外なら該当localeの
  messages.jsonを直接読み込んで適用する。読めない場合は従来表示に
  フォールバック。
- メモ：v3.48で入れた「前面に来るまでグリッド初期化を待つ」ゲートが、
  ユーザーが他タブを使用中の自動テストでも正しく発動することを確認
  （バックグラウンドのタブでは意図通り待機する）。

## v3.53.0：UI言語をXの表示言語に追従・フィルタ設定の永続化・キー操作いいねのタイル上フィードバック

- **拡張のUI言語を「ブラウザの言語」ではなく「Xの表示言語」(&lt;html lang&gt;)に
  追従するよう変更**（実機フィードバック：ブラウザは日本語のままXだけ英語に
  している場合、「Xは英語なのに拡張だけ日本語」になっていた）。content.js
  にja/en両方の文言テーブルを内蔵し、Xがja以外なら英語で表示。英語Xでの
  表示を実機確認済み（"Images only (For you): ON"等）。設定ページ
  (options.html)だけは従来通りブラウザ言語（chrome.i18n）に従う。
- **「未読のみ表示」「動画のみ表示」のON/OFFを永続化**（実機フィードバック
  「毎回リセットされる」への対応。以前は意図的に非永続だった）。
- **キー操作でいいね等をした時、対象タイルのボタン列を1.5秒表示して
  フェードアウト**（実機フィードバック：画面下のトーストだけだと、どの
  タイルにいいねしたのか分かりにくい）。実行結果の色（ピンク/青/緑）が
  そのタイルの上で直接見える。
- メモ：拡張を再読み込みした直後は、既に開いているXタブのスクリプトが
  旧版のまま切り離され、⚙設定ボタン等（background通信を使う機能）が
  反応しなくなる。**再読み込き後は開いているXタブをF5すること**（Chromium
  拡張の一般的な挙動で、この拡張固有のバグではない）。

## v3.52.0：多言語化（日本語/英語、ブラウザ言語で自動切替）

- **拡張自身の全UI文言（107キー）をchrome.i18n（_locales方式）で多言語化**：
  日本語がdefault_locale、英語訳を同梱。ブラウザのUI言語が英語なら
  ツールバー・トースト・ビューア・設定ページ・ストア説明文まで全部英語に
  なる（chrome.i18nの標準動作）。日本語環境では従来と完全に同一表示
  （実機確認済み）。英語表示は日本語ブラウザでは確認不能のため未検証。
- **実機で発見した罠と対策**：`_locales`/`default_locale`を「後から追加」
  した未パッケージ拡張は、runtime.reload()（Extensions Reloader経由）では
  ロケールカタログが再構築されず、getMessageが空を返し続ける（UIがキー名
  だらけになる）。brave://extensionsからの手動再読み込みで直る見込みだが、
  保険として**日本語フォールバック表をcontent.jsに内蔵**した（ja/messages.
  jsonから機械生成・内容同一）。カタログが読めない状態でも常に日本語で
  表示され、キー名がユーザーの目に触れることはない。
- options.htmlは`data-i18n`属性方式（HTML内の日本語がそのまま
  フォールバック）。「このポストを開く（R）」等のキー表記はリマップに
  追従するようプレースホルダ化（副次的な挙動改善）。

## v3.51.0：Xの表示言語に依存しない実装へ（英語UI対応の土台）・検索メディアの追加読み込み修正

- **検索メディアの追加読み込みが33件で止まる不具合を修正**（実機発見）：
  検索メディアの仮想リストは約11個のセル要素を使い回すため、`xmrDone`印が
  「昔の中身の時に付いた印」として残り、追加読み込み分が永遠にスキップ
  されていた。検索モードでは`xmrDone`を使わず画像リンク単位の照合だけで
  重複排除するよう変更。※検証中にXの検索レート制限（「問題が発生しました」）
  に当たったため、修正後の増加確認は次セッション持ち越し。
- **Xの画面文言（日本語）に依存していたロジック7件を言語非依存化**
  （読み取り専用エージェントによる全数棚卸しに基づく。英語UIでの動作は
  実機未検証だが、日本語UIでの回帰テストは全項目パス）：
  1. ホーム更新の新着バナー検出：構造判定（数字+アバターサムネの小ピル）を
     第一条件に、日本語正規表現はフォールバックとして残す
  2. フォローボタンの状態判定：文言比較(`!== 'フォロー'`)→testid判定
     (`-unfollow`)。英語UIで常に「フォロー中」表示になる誤動作を予防
  3. ホームタブのON/OFF設定キー：表示文言→位置ベース(tab0/tab1)+リスト名。
     旧日本語キーからの移行読み込みつき（実機確認済み）。UI言語を変えても
     設定が消えなくなった
  4. メディア欄の可変タブ除外・いいね/ブックマークタブ除外：文言→href判定
  5. 「動画」ピルの特別処理：ラベル文字列→isVideoPillフラグ（i18n化準備）
  6. 「フォローされています」行：主要言語OR（脆い暫定対応と明記）
- 狭いウィンドウ対策：ビューアのキャプション幅を`min(280px, 35vw)`に。
- メモ：動画0件のアカウントの動画タブではセルが存在しないため、従来通り
  グリッド化せずXの素のページを表示する（正しい挙動と確認）。

## v3.50.0：いいね数・リポスト数・リプ数・表示回数を表示・ホバーボタンは「未実行=グレー/実行済み=色」に変更

- **エンゲージメント数の表示**（実機フィードバック「いいね数とかRT数とか
  どこにも書いてない」への対応）：収穫時に裏のセルの各ボタンから数字を
  読み取る（ボタンのtextContentは"1.2万"等の数字表示そのものなので、Xの
  表示言語に依存しない）。タイルの帯の右端に主要3種（♥/🔁/💬）を
  コンパクトに、拡大表示には表示回数込みの4種（💬/🔁/♥/👁）を表示。
  実機確認済み。検索メディア由来のタイルは数字を取得できないため非表示。
- **ホバー操作ボタンの色を「未実行=グレー、実行済みだけ機能色で塗り」に
  変更**（実機フィードバック：未実行でうっすら色が付いているのは
  「押したら色が変わる」の直感に反する、との指摘。v3.49.0の薄色ベースを
  撤回）。

## v3.49.0：ホバー操作ボタンを機能別に色分け・実行済みは塗りつぶし表示・空アバターの丸を非表示

- **ホバー操作ボタン（♥/🔖/🔁）を機能ごとに色分け**（実機フィードバック
  「グレーの丸だと見分けがつかない」「ブックマーク済みか分からない」への
  対応）：薄い色=未実行、濃い塗りつぶし=実行済み、の2段階。いいね=ピンク、
  ブックマーク=青、リポスト=緑（Xの本物の配色に合わせた）。ブックマーク
  一覧で「全タイルBM=青塗り＋いいね状態が個別に反映」を実機確認済み。
- **アバターが取得できないタイル（検索メディア等）で空のグレー丸が
  出ていたのを非表示に**（タイルの帯・拡大表示の両方）。
- **検索メディアでホバーボタンが出ない件について（仕様）**：検索メディアの
  裏のセルは実機DOM検証の通りいいね等のボタン自体を持たない構造のため、
  押しても実行できないボタンを出すより非表示が正直、という判断。操作
  したい時はRキーでツイートを開く（キーを押すとその旨のトーストが出る）。
- **「検索→ホーム復帰でONなのにグリッドにならない」報告について**：
  v3.49時点の実機再現試験（フォロー中タブON→Xの本物の検索フロー→
  メディアタブ→ホームリンクで復帰）では1秒でグリッド化し再現せず。
  v3.44の起動デッドロック修正＋v3.48のバックグラウンド待機で塞がった
  可能性が高い。再発したら発生直前の操作を記録すること。

## v3.48.0：バックグラウンドタブで開くとグリッドが空になる不具合を修正・検索ボタンを右端へ・トグルON時の色表示・バッジ/ボタンのツールバー貫通修正

- **【実機報告】Fキーで新しいタブに開いた後、そのタブを見ずに他の画面を
  見ているとグリッドが空のまま（リロードで直る）不具合を修正**：原因は
  バックグラウンドタブへのブラウザのスロットリング（タイマーが大幅に
  間引かれ、Xの仮想リストもスクロールに反応しない）で、初期化が全部
  空回りしていた。対策として、タブが前面に来るまで初期化自体を開始しない
  ようにした（visibilitychangeイベント駆動なので前面に来た瞬間に始まる）。
- **「検索」ボタンをツールバーの一番右（設定グループ末尾）へ移動**：
  Xの本来のUIで検索は右上にあるため位置の直感を合わせた。押すと右側の
  サイドバーが一時的に出る仕組みとも位置が繋がる。
- **トグル系ボタン（未読のみ/動画のみ/画像のみ表示）のON状態を色でも表示**
  （Xブランド色の背景。ホームタブのアクティブ表示と同じ配色言語）。
- **複数枚バッジ(1/4)やホバー操作ボタンが、スクロール時に上部ツールバーを
  貫通して見えていたのを修正**：ツールバーのz-indexが1のままタイル側の
  z-index(2〜5)に負けていた。10に引き上げ。

## v3.47.0：リポストキー(5)・W/A/S/D/Qもリマップ可能に・キー割り当てなし対応・ツールバーをグループ整理・検索結果のメディアタブ対応・画像のみ表示トグルをツールバーへ

- **リポスト（デフォルト5キー、タイル/ビューアの🔁ボタン、リマップ可）**：
  Xのリポストは「ボタン→確認メニュー→リポスト」の2段階UIのため、キー
  1押しで確認まで通す。引用リポストは対象外（文章入力が必要。引用したい
  時はRキーで開いてから）。リポスト済みはXと同じ緑で表示。確認メニューの
  DOM構造（retweetConfirm/unretweetConfirm）は実機検証済みだが、**最後の
  確認クリックまで通す実行自体は未テスト**（テストで実際にリポストしない
  方針のため）。初回はユーザーが1件で試して確認のこと。
- **W/A/S/D/Q（移動・開閉）もリマップ可能に**：Space/Esc/矢印キーは固定の
  まま。「•••」メニュー内の移動・通常タイムラインの選択もリマップに追従。
  ビューアのヒント文言もキー設定から動的に組み立てる。既存の保存設定とは
  互換（未設定分はデフォルトが使われる）。
- **キー割り当て「なし」対応**：設定でアクションキーを空欄にすると
  そのキーは無効になる（例：リポストを使わない人の誤爆防止）。移動・開閉の
  5キーは必須のまま。スペース文字の割り当ては固定Space操作と衝突するため
  弾く。
- **ツールバーを4グループに整理**（実機フィードバック「渋滞気味」への対応）：
  ①ナビ（いいね/ブックマーク/検索）②フィルタ（更新/未読のみ/動画のみ/
  画像のみ表示）③タブ（アカウント固有タブ・ホームのタブ）④設定（列数/⚙、
  右端寄せ）。薄背景の囲みでグループを視覚化し、区切り線は廃止。自作リスト
  が多い場合もグループ内で折り返して壊れないようにした。
- **「画像のみ表示」トグルの位置を改善**（実機フィードバック「左下
  フローティングがださい」）：グリッド表示中はツールバーのフィルタ群に
  収まり、グリッドOFF中（ONに戻すボタンが必要な時）だけ従来の左下
  フローティング。ON/OFF往復で正しく行き来することを実機確認済み。
- **ワード検索結果の「メディア」タブ（/search?q=…&f=media）に対応**：
  実機DOM検証で、検索メディアは「1セル=画像最大3枚の行・articleも投稿者
  情報も無い」特殊構造と判明。専用の収穫処理を実装し、@ハンドルをhrefから
  復元して帯に表示。グリッド・WASD・ビューア・R/Fキーは全て動作（実機
  確認済み）。いいね等の直接操作はセルに委譲先が存在しないため構造上
  不可能で、押すと正直にその旨をトースト表示してRキーへ誘導する。
  ホバーボタン列も出さない。
- **動画タイルの再生アイコンを1.5倍に拡大**（40px→60px）。

## v3.46.0：タイルにホバー操作ボタン・いいね済みの色表示・ビューアからのリプライ表示バグ修正・ヘッダー画像・その他UI改善（全て実機検証済み）

- **ビューアからリプライキーを押してもモーダルが見えないバグを修正**
  （実機報告：「押しても出てこない、でも戻ると出てくる」）：モーダル表示中に
  隠すのをグリッド本体(.xmr-shell)だけにしていて、その上に重なる拡大表示
  オーバーレイ(.xmr-overlay)を隠し忘れていた。両方隠すよう修正し、
  モーダルを閉じればビューアが選択位置ごと復帰することも実機確認済み。
- **タイルにホバー時の操作ボタン列（♥/🔖/💬）を追加**：マウスだけで
  いいね・ブックマーク・リプライが完結する。設定ページでON/OFF可
  （初期値ON）。クリックしてもビューアは開かない（伝播停止）。
- **いいね/ブックマーク済みの状態を色で表示**：Xの本物のUIと同じ配色
  （いいね済み＝ピンク、ブックマーク済み＝青）。状態は収穫時（裏のセルが
  マウントされている瞬間）に読み取り、以後は自前の操作時に更新する。
  他の端末で変えた分までは追わない（そのためだけの再マウントはコストに
  見合わないため）。タイルのボタンとビューアのボタンの両方に反映。
- **ビューアのアイコン/名前クリックでそのアカウントのページを新しいタブで
  開くように**（タイルの帯のアバターと同じ挙動に統一）。
- **メディア欄のプロフィールカードにヘッダー（バナー）画像を表示**
  （あるアカウントのみ。最大140px・角丸・カード最上部）。
- **「マウスホイールを一度スクロールしてみてください」ヒントを削除**：
  リロード方式の導入でほぼ到達しない上、グリッドが空の状態ではホイールを
  回しても何も起きずヒントとして不正確だったため（実機フィードバック）。
- 内部整理：いいね/BM/リプライの「entry→本物のセルの解決＋一時再マウント」
  処理を共通ヘルパー(remountArticleForEntry/actOnEntry)に一本化。

## v3.45.0：debugger権限を完全削除・メディア欄でもいいね/BM可能に・リプライキー(4)新設・拡大表示に操作ボタン列（全て実機検証済み）

- **debugger権限を完全削除**：CDP擬似スクロールは実機で「message port
  closed」により時々失敗し、効果自体も未確認のままだった。確実に効くと
  実機確定済みのリロード方式(`tryResyncReload`)に一本化し、`background.js`の
  `simulateWheelScroll`一式・manifestの`debugger`権限・設定ページの説明文を
  全て削除した。権限は`tabs`と`storage`だけになり、「デバッグしています」
  警告バーは二度と出ない。無限リロード防止としてsessionStorageで同一URLへの
  再同期リロードを5分に1回に制限し、`#xmr-fresh`のFキーリロードとも
  ガードを共有（二重リロードは起きない）。リロード直前には既読データの
  2秒デバウンス分をflushしてから実行する。
- **メディア欄でもいいね/ブックマークが効くように**：「メディア欄のセルは
  いいねボタンを持たない」という以前の想定は誤りで、実機DOM検証で
  like/bookmark/reply/retweetを全部持つと確認。除外を撤廃した。
  実機テスト済み（いいね→解除→復元のトグル動作、トースト表示とも正常）。
- **リプライキー（デフォルト4、リマップ可）を新設**：選択中の投稿の本物の
  リプライボタンへclick委譲し、Xの標準リプライ入力モーダルを**ロード0秒で**
  開く。入力・送信は完全にXの本物のUIでユーザー自身が行う（この拡張は
  「開く」だけ。表示専用の大原則の範囲内）。実装上の発見2つ：
  ①Xのモーダル層(#layers)はz-index:1で、親のスタッキングコンテキストの
  制約からz-indexを上げてもグリッド(z:40)を超えられない→モーダル表示中
  だけシェルをvisibility:hiddenにする方式で解決（スクロール・選択位置は
  保持され、閉じると完全にそのまま復帰する。実機確認済み）。
  ②/compose/…はモーダル用の仮想URLで下のページは生きているため、
  onNavigateがこのURLで通常の遷移処理（グリッド解除→再構築）をしないよう
  ガードを追加。
- **拡大表示（Qで開く画面）のキャプションに「♥いいね/🔖ブックマーク/
  💬リプライ」の可視ボタン列を追加**：キー操作と同じ処理をマウスでも。
  実機確認済み（クリックでトグル動作、ビューアは開いたまま）。

## v3.44.0：スクロール時の追加読み込みを高速化・グリッドが二度と起動しなくなるデッドロックを修正

- **追加読み込みの高速化（実機報告「ロードが0.3秒くらい遅い」への対応）**：
  - pumpMoreの固定`sleep(300)`を廃止し、「裏のリストに新しいセルが追加
    されたら即続行、来なければ従来通り300msで諦める」適応待ち
    (`waitForSourceGrowth`)に変更。300msという数字に並び順保証の意味は
    無い（順序は末尾追加＋translateYで担保）ことを確認した上での変更。
    実機計測：初回13枚の表示が数秒→**0.5秒**に短縮。
  - 実装中に一度退行を出した：新セル追加「以外」のDOM変化（セルの
    アンマウント等）でも待ちが即解除されてしまい、「新着なし」判定が
    速く回りすぎて初回読み込みが途中で打ち切られた。シグナルを
    「sourceRoot直下へのaddedNodes」だけに限定して修正。併せて打ち切り
    判定を回数ベース(10回)から時間ベース(最終増加から4秒)に変更。
  - 先読み開始を下端800px→2画面分手前に拡大。WASD側の先読み閾値も
    12件固定→列数×4行に。タイル画像を取り込み時点でHTTPキャッシュへ
    先行ロード（白タイル対策）。
- **v3.42.0で入れた重複起動ガードのデッドロックを修正（実機で発生）**：
  進行中フラグ(activatingMode)が、activateGrid本体の途中return経路
  （body待ち失敗・先頭セル不発等）で解除されず、その URLでは以後二度と
  グリッドが起動しなくなっていた。try/finallyのラッパーで全経路確実に
  解除するよう修正。
- **【未解決・次回最重要】Xの仮想リスト自体が壊れる状態を実機で特定**：
  メディア欄でスクロール最下部にいてGraphQLフェッチ(UserPhotoTimeline)は
  200で成功するのに、リストの高さが伸びず新セルがマウントされない状態を
  再現。リロードしても深いスクロール位置の復元で再発する。この状態では
  マウント済みセルに画像も描画されず、harvestNewが「画像なし」と誤分類して
  永久に捨てる二次問題もある。詳細と調査方針は予約タスク
  (x-media-restore-resume-0341)に記載。

## v3.43.0：グリッド・拡大表示から直接いいね/ブックマークできるように（実機検証済み）

- **報告**：「今は画像を開いているだけでツイートを開けていないので、いいねが
  押せず、いちいちポストを見に行かないといけない。ここが明確な弱点」。
- **実装**：グリッド表示中・拡大表示中に2キー(いいね)/3キー(ブックマーク)で、
  選択中の投稿の裏に隠れている本物のいいね/ブックマークボタンへclick()を
  委譲する。ツイートページを開く必要がないので**ロード0秒**。既にいいね済み
  なら解除（トグル動作）。実行結果は画面下のトーストで必ず表示
  （「いいねしました」「いいねを解除しました」等）。
- **実装中に実機で見つけた問題と対策**：グリッドの初期読み込みは裏のリストを
  下までスクロールするため、上の方のタイルの本物のセルはほとんどの場合もう
  アンマウントされていて、単純なclick委譲はほぼ常に失敗する。各entryが
  収穫時のtranslateY座標を持っていることを利用し、**キーが押された瞬間だけ
  裏のリストをその座標へ一時スクロールしてセルを再マウントさせ、click後に
  元の位置へ戻す**方式にした（設計原則6の大ジャンプ禁止は「未収穫セルの
  喪失」を防ぐ原則なので、収穫済み座標への一時ジャンプは対象外）。
- **実機検証**：Extensions Reloader導入により初の完全自走PDCA
  （コード修正→ http://reload.extensions →x.comで実機テスト）で検証。
  ホームグリッドでアンマウント済みの選択投稿に2キー→「いいねしました」、
  再度2キー→「いいねを解除しました」、スクロール位置復帰、まで全て確認済み。
- 安全設計は従来通り：実行されるのはユーザーがキーを押した時だけ。
  見つからない場合は自動で探し回らず正直にトーストで伝える。誤爆防止の
  ため、グリッド表示中に対象が見つからなくても無関係な記事へは
  フォールバックしない。
- デバッグ支援として各タイルに対応ツイートのhrefをdata属性
  (`data-xmr-href`)で持たせた（実機検証の突き合わせ用）。

## v3.42.0：他ページからホームへ戻った時、画像のみ表示ONなのにグリッド化されないことがある不具合を修正（未検証）

- **報告**：「画像のみ表示on/offをポチポチしただけじゃ切り替わらない時が
  ある？他からhomeに帰ってきたとき」との指摘。
- Claude-in-Chromeで実機再現を試みたが、テスト環境のタブが
  `document.hidden=true`（バックグラウンド扱い）のままで、以前
  「検証の壁」として記録した非フォアグラウンドタブの挙動不安定
  （setTimeout遅延・仮想リスト更新停止）と同じ制約に当たり、クリーンな
  再現は取れなかった。**この修正は実機確認できていない**。
- ただしコードを読み直した結果、条件が揃えば起こり得る具体的な競合状態
  (race condition)を見つけたため、確認できないまま放置せず先に修正した：
  ホームへ戻った直後、Xがタブ一覧(`role="tablist"`)のDOM要素を短時間に
  複数回作り直すことがある（スケルトン→本描画等）。ページ全体を監視する
  `bodyObserver`はこの度に`ensureHomeTabObserver()`を呼び直し、「新しい
  タブ一覧が見つかった」と判定するたびに`activateGrid('home')`を呼んで
  いた。ところが`activateGrid()`は呼ばれるたびに`Grid.navToken`を進め、
  前回の呼び出しを「もう古い」として打ち切る設計になっているため、短時間に
  何度も重複して呼ばれると、どの呼び出しも完了できないまま次の呼び出しに
  打ち切られ続け、結果として`Grid.active`がfalseのまま（＝トグルの表示は
  ONなのに実際はグリッド化されない）ということが理論上起こり得た。
- **修正**：`Grid.activatingMode`/`Grid.activatingHref`という新しい状態を
  追加し、「今まさに同じmode・同じURLへ向けて進行中の呼び出しが既にある
  場合は、新たに呼ばれても何もせず進行中の方に任せる」という重複排除を
  `activateGrid()`の先頭に追加した。既存の「実際に状態が変わった時は
  ちゃんと打ち切って新しい方を優先する」という`navToken`の仕組み自体は
  変えていない（mode/URLが違う場合は今まで通り打ち切られる）。

## v3.41.0：画像のみ表示ON/OFFでの並び順の不具合を実機テストで発見・修正

- **前回（v3.40.1）はコードレビューのみで「変更なし・維持されている」と
  報告したが、実際にTwitter上で実機テストして確かめてほしいとの指摘を受け、
  Claude-in-Chromeでx.comのメディア欄を開き、マウスホイールで実際に
  スクロールしながらグリッドのタイル順と裏の本物のセル順（DOM上に隠れて
  残っている）を突き合わせて比較した。**その結果、並び順が一致せず
  30秒以上待っても直らないケースを実際に再現できた。**
- **原因**：初回読み込み完了時に1回だけ並び順を整える`settleGridOrder()`を
  「ユーザーがまだ操作を始めていないか」で判定していたが、この判定に
  使っていた`Grid.maxSeenIndex > 0`は、WASD等の意図的な操作だけでなく、
  マウスホイールでの受動的なスクロール追従（`updateSelectionFromScroll()`）
  でも上がってしまっていた。つまり、読み込み中に一度でもマウスホイールで
  スクロールすると（冷起動時のヒント「マウスホイールを一度スクロールして
  みてください」通りの操作をしただけでも）、並び替え自体がスキップされて
  順番が揃わないまま、という抜け穴があった。
- **修正**：`maxSeenIndex`とは別に、WASD/A/D/Spaceでの意図的な操作でのみ
  trueになる専用フラグ`Grid.userHasNavigated`を新設し、判定をこちらに
  差し替えた（該当箇所2か所：メディア欄初回読み込み時／ホーム更新時の
  どちらも）。ページ移動時・グリッド作り直し時にはこのフラグをリセットする。
- 併せて、実機テスト中に`chrome.debugger`を使ったスクロール補助
  （`requestSimulatedScroll`）が「message port closed before a response
  was received」で時々失敗する場面も観測した。今回の並び順の不具合とは
  別件で、まだ未調査・未修正。今後の課題として記録しておく。

## v3.40.1：設定ページにdebugger権限の説明を追加・並び順の実装を再確認

- **設定ページに`debugger`権限についての説明を追加**：Xの読み込み位置が
  ずれた時に本物のスクロール操作を送り直すためだけに使っていること、
  使われた瞬間だけ短時間の警告バーが出ること、外部送信はしないことを明記。
- **画像のみ表示ON/OFFでの並び順（過去に何度も再発した最も根深い不具合）
  を再確認**：中核となる`pumpMore()`（1画面分ずつの小刻みスクロール、
  大ジャンプ厳禁）・`appendEntry()`（常に末尾追加、途中挿入しない）・
  `settleGridOrder()`（初回読み込み完了時に1回だけtranslateY順に並べ直す）
  のコードを読み直したが、修正時から変更は無く、この設計は維持されている
  ことを確認した（実機での再テストではなくコードレビューによる確認）。

## v3.40.0：Fキーで開いたメディア欄が古い内容のまま表示される不具合を確実な方法で修正・複数画像一覧がはみ出て下が見えない不具合を修正・拡大表示のちらつきの残りを修正・設定ページの文章調整

- **Fキーで新しいタブに開いたメディア欄が最新の投稿を反映しない不具合を、確実な方法で修正**：
  これまでの「もう一度だけ強制的にスクロール入力を送る」という対策では
  直らなかった（実機報告）。原因を確実に断つ手段が無かったため、方針を
  変え、**確実に効くと分かっている「リロード」を新しいタブの初回表示直後に
  自動で1回だけ行う**ことにした。Fキーで開くURLに`#xmr-fresh`という印を
  付けておき、content scriptの一番最初（他の初期化より前）でこの印を検知
  したら、印を外してから`location.reload()`する。ごく短い自動リロードが
  一瞬挟まるトレードオフはあるが、内容が古いままになるよりはこちらを優先。
- **複数画像（4枚等）の一覧が画面をはみ出す時、下側にスクロールして見られない不具合を修正**：
  オーバーレイが`justify-content: center`で常に中央寄せする作りだったため、
  はみ出した分を見るためのスクロール自体ができなかった。収まる時は中央
  寄せのまま、はみ出す時は先頭寄せに切り替わりスクロールで全部見られる
  `safe center`に変更し、`overflow-y: auto`も追加した。
- **拡大表示を初めて開いた瞬間だけキャプションがちらつく不具合を修正**：
  前回、画像を切り替える時のちらつきは直したが、グリッド一覧から初めて
  開く瞬間（画像要素を新規に作る一番最初の1回）は対象外だったため、まだ
  残っていた。画像に下限の幅を設けて、読み込み前でもある程度の場所を
  確保しておくようにした。
- **設定ページの文章を微調整**：「見た目を変えるだけで勝手に投稿しない」
  という一文は、そもそもそういう機能ではないと分かるので不要との指摘を
  受けて削除。列数を変更できる旨は「グリッド表示」の説明に移動（個人的に
  重要とのことなので「その他」から昇格）。

## v3.39.0：ツールバーの「検索」ボタンを大きく・Space長押し時の残りのがくつきを修正

- **ツールバーの「検索」ボタンを一回り大きくした**：前回「検索ボックスが
  小さい」との報告を受けてサイドバー側の検索入力欄を広げたが、実際に
  指していたのはツールバーの「検索」ボタン（押すとサイドバーを一時的に
  表示するボタン）の方だったと判明。他のツールバーボタンより大きく
  （文字サイズ・パディングとも拡大）して押しやすくした。
- **Space長押し時、少し改善したもののまだ残っていたがくつきを修正**：
  単押し(repeat:false)で始まる滑らかなscrollBy()アニメーションが、長押し
  判定に切り替わった後もしばらく動き続けていて、新しく始まる一定速度の
  requestAnimationFrameループとscrollTopを取り合っていたのが原因。長押し
  ループを始める直前に、今の位置へアニメーション無しで入れ直すことで
  進行中のアニメーションを打ち切ってから切り替えるようにした。

## v3.38.0：検索ボックスを少し広く・Space長押し時のスクロールをなめらかな一定速度に変更

- **検索ボックスを少し横に長くした**：DOM上の位置・親子関係は動かさず、
  CSSで見た目の幅だけ広げた（サイドバー自体の幅を超えてあふれないよう、
  固定pxではなく上限付きで親の幅いっぱいまで広げる形）。
- **Spaceキーを長押しした時、がくがくしながらゆっくり進む不具合を修正**：
  単押しのたびに滑らかなscrollBy()アニメーションを呼んでいたが、長押しで
  OSのキーリピートが大量のkeydownを発火させると、前のアニメーションが
  終わる前に次が割り込んで打ち消し合っていたのが原因。`KeyboardEvent.repeat`
  で単押しと長押しを区別し、長押し中は都度アニメーションを起こすのではなく
  requestAnimationFrameで経過時間に応じてスクロール位置を直接動かす、
  一定速度の方式に切り替えた（速度は単押しで届く距離感の中間くらいを目安に
  設定。CONFIG.spaceHoldScrollPxPerSecで調整可能）。

## v3.37.0：「•••」メニューの選択枠線を自前CSSで確実に表示・ホーム復帰時に画像のみ表示が反映されない不具合を修正・設定ページの文章を整理

- **「•••」メニューでW/S移動中に選択枠線が見えなくなる不具合を修正**：
  内部の選択位置自体（`.focus()`）は正しく動いていたが、ブラウザの
  `:focus-visible`判定がプログラム的な`.focus()`呼び出しに対して常に
  見た目のリングを出してくれるとは限らないため、見えたり見えなかったり
  していた。グリッドの他の選択枠と同じ、自前のCSSクラスで確実に表示する
  ようにした。
- **他のページからホームへ戻った時、「画像のみ表示」がONのはずなのに
  グリッド化されず、更新等の別操作をして初めて反映される不具合を修正**：
  ページ遷移直後にはXがまだホームのタブ一覧を描画し終えていないタイミングが
  あり、その一覧を探す処理が見つからないまま即座に諦めていたのが原因と
  考えられる。少し待って再度探し、見つかった時点で状態を再チェックする
  ようにした。
- **設定ページの文章を整理**：サイドバー設定の説明が長すぎて分かりにくいと
  の指摘を受けて簡潔にした。右側の機能紹介パネルも、内部の実装詳細
  （DOM操作の仕組み等）を減らし、この拡張機能を配布された場合に読む人へ
  向けた分かりやすい説明に書き直した。

## v3.36.0：「•••」メニューのW/S/Q操作を作り直し（合成キーイベントは効かないと判明）・検索の予測候補をクリックできるように修正・メディア欄が古い内容のまま表示される不具合の対策

- **「•••」メニューのW/S（移動）・Q（閉じる）操作を、合成キーイベントに頼らない方式に作り直した**：
  実機報告で「Spaceは確定できるがW/Sでの移動やQでの閉じるができない」と
  判明し、改めて実機検証したところ、**Xの「•••」メニューはArrowUp/
  ArrowDown/Escapeについて信頼済み(isTrusted)の本物のキー入力にしか反応せず、
  content scriptから送れる合成KeyboardEventには一切反応しないことを確認した**
  （以前「反応する」としていたのは誤りで、信頼済み入力での動作確認と
  混同していた）。一方、メニュー項目への`.focus()`や`.click()`、メニュー
  外側の要素（バックドロップ）への`.click()`は合成イベントでも問題なく
  機能することを確認済みだったため、キー操作をネイティブのキーボード
  ナビゲーションに頼らない方式に作り直した：選択中の項目は自前で管理し、
  W/Sでその項目を`.focus()`（見た目のハイライトも一致する）、Spaceで
  `.click()`、Qでメニューのバックドロップ要素を`.click()`して閉じる。
- **検索の予測候補をクリックしても検索されずサイドバーが消えるだけの不具合を修正**：
  一時的な検索表示を閉じるかどうかの判定（クリック位置が検索の範囲内か）が、
  検索入力欄そのものの祖先だけを見ていたため、予測候補の一覧
  ([role="listbox"])が実は入力欄の子孫ではなく兄弟として描画されている
  ことを見落としていた（実機確認）。範囲の判定を、入力欄を含む`<form>`
  （予測候補一覧もこの中に描画される）に変更した。
- **一覧からFキーでメディア欄を新しいタブで開いた時、最新の投稿が表示
  されないことがある不具合に対策**：Xが古いスクロール位置のスナップショットを
  「先頭」として復元してしまい、既存の判定（先頭セルの位置だけを見る）
  では気づけないケースがあると推測される（実機報告：リロードすると直る）。
  先頭と判定した直後にもう一度だけ強制的に本物のスクロール入力を送り、
  Xに再同期の機会を与えるようにした。**この対策の実際の効果は未確認**。

## v3.35.0：「常時隠す」設定の間だけ出る一時的な検索ボタンを追加・設定ページの説明ミスを修正

- **hideSidebar設定がONの間だけ、グリッドのツールバーに検索ボックス風の
  「検索」ボタンを追加**：押すと一時的に本物のサイドバーを表示し、検索
  ボックスへ自動フォーカスする。この一時表示はどこにも保存されず、検索
  以外の場所をクリック/フォーカスしたり、検索をかけてページ移動したり
  すると、次にグリッドへ戻ってきた時には元通り隠れている（実機報告：
  「サイドバーを常時隠したい人でも、検索したい時だけ検索ボックスを出して、
  それ以外は隠れたままにしたい」という要望を受けて追加）。v3.34.0で
  ツールバーに追加していた常時ON/OFFのトグルボタンはこの一時ボタンに
  置き換え、常時ON/OFFの切り替え自体は設定ページのチェックボックスのみで
  行うようにした（サイドバーを隠していない時はそもそも検索ボックスが
  常に見えているので、このボタン自体も出さない）。
  ボタンの位置は、ホームのタブ切替ピル（フォロー中／リスト等）の右、
  列数入力の左に配置。
- **設定ページの機能説明パネルの誤りを修正**：「•••」メニューのキーが
  「興味がない」を直接実行できるかのような書き方になっていた（実際は
  メニューを開くところまでで、選択自体は本物のメニュー項目をユーザーが
  行う）。実機報告「興味がないのボタンは無い、•••を押せるだけ」を受けて
  訂正。

## v3.34.0：サイドバー表示を「隠すか隠さないかの二択」に戻し設定を復活・ページ内トグルボタンを追加・未読/動画のみ表示時のSpace/スクロール列ズレを追加修正・設定ページに機能全体の説明パネルを追加

v3.33.0の「検索ボックスだけ画面右上に固定表示」はユーザーから明確に差し戻された（「表示するなら全部表示してほしい、設定自体は残してほしい」）。技術的には動いていたが、求められていたものとは違った。

- **hideSidebar設定を復活し、「隠すか隠さないかの二択」のシンプルな方式に戻した**：
  ONなら右サイドバーを丸ごと隠して横幅をグリッドに使う。OFFならサイドバーには
  一切手を加えず、Xの標準表示のまま横に並べる（検索ボックスだけを固定表示する
  等の部分的な加工はしない）。
- **サイドバーの表示/非表示を、ページ内のツールバーのボタンからもページ
  リロード無しで即座に切り替えられるように追加**：設定ページのチェック
  ボックスとchrome.storageで同期する。「設定は残してほしいが、可能なら
  ページ内のボタンでリロード無しに切り替えたい」という要望に対応。
- **「未読のみ表示」「動画のみ表示」が有効な時、Space/マウススクロールで
  選択の列がズレる不具合を追加修正**：v3.32.0で「列を保つ判定自体をやめて
  中央に一番近いタイルを探す」方式にしていたため、縦スクロールしただけで
  別の列に飛んでしまっていた。列を保つこと自体はやめず、判定方法を配列
  インデックスではなく実際の描画位置（X座標）に変更した（選択中タイルの
  中心X座標に近いタイルだけを候補にする）。
- **右サイドバーの「いま」を見つけようウィジェットをいいね/ブックマーク
  ボタンに置き換える古い機能(`pruneSidebar`)を撤去**：ツールバーに同じ
  導線が既にあり重複していた。付随していた「サイドバーの`<aside>`要素を
  常時強制的に隠す」CSSも撤去（グリッドの状態に関係なく常時発動しており、
  「隠さない設定でも何か消えている」の原因だった可能性が高い）。
- **設定ページ（options.html）の右側の空きスペースに、拡張機能全体が
  Xに対して何を変更しているかを重要度順にまとめた説明パネルを追加**：
  何度も細かい変更を重ねてきた結果、全体像が分かりにくくなっていたため。

## v3.33.0：検索ボックスを画面右上に固定表示（サイドバーの他の要素は隠す）・hideSidebar設定を撤去・Rキーで開いた投稿を自動選択・旧サイドバー整理機能を撤去

- **右サイドバーの検索ボックスだけを画面右上に固定表示するように変更（グリッドは常にフル幅）**：
  これまでの「サイドバーを隠すか隠さないか」という二択（隠さない場合は
  その分グリッドが狭くなる）をやめた。実機検証の結果、検索ウィジェットを
  DOM上の親からは一切動かさず、CSSの`position:fixed`だけで画面右上に
  固定しても、内部の予測候補パネルは新しい位置に正しく追従して開くことを
  確認できた（過去に「動かすと壊れる」と分かっていたのは、DOM構造その
  ものを別の場所へ移動させた場合の話で、見た目の位置だけをCSSで変えるのは
  別物だった）。これによりグリッドは常にフル幅、検索ボックスだけを画面
  右上に小さく固定表示し、トレンドやおすすめユーザー等の他のウィジェットは
  隠す、という「検索バーだけ欲しい」という要望に一番近い形にした。
- **設定の「グリッド表示中は右サイドバーを隠す」を撤去**：上記の変更で
  常に同じ（フル幅＋検索ボックスのみ固定表示）になったため、このON/OFF
  設定自体が意味を持たなくなった。設定ページからチェックボックスを撤去。
- **Rキーで新しいタブに開いたポストを自動で選択するように変更**：今まで
  Rキーでツイートを開いた直後、2/3キー（いいね/ブックマーク）を使うには
  Qキーを1回押してそのポストを選択する必要があった。ツイート単体ページ
  （投稿は常に1件だけ）に限り、記事が表示され次第自動で選択しておくよう
  にした（複数の投稿が並ぶページでは意図しない選択を避けるため対象外）。
- **右サイドバーの「いま」を見つけようウィジェットをいいね/ブックマーク
  ボタンに置き換える古い機能を撤去**：ツールバーに同じ導線が既にあり、
  重複していた（このリンクは既にツールバー等の別の場所にあるため、との
  指摘を受けて撤去）。また、この機能に付随していた「サイドバーの
  `<aside>`要素を常時（グリッド未使用時も含めて）強制的に隠す」CSSも
  合わせて撤去した。これが実機報告「サイドバーを隠さない設定でも何か
  消えている気がする」の原因だった可能性が高い（グリッドの状態に関係なく
  常時発動する隠しルールだったため）。

## v3.32.0：ホームへ戻るキー・未読/動画のみ表示のD/A折り返しとScroll/Spaceのランダム移動を修正・読み込み後の二重リセットを修正

- **ホームへ1キーで戻れるように追加（デフォルト`1`、設定でリマップ可）**：
  「このページで見たいものは全部見たのでTLに戻りたい」という時用。本物の
  「ホーム」ナビアイコンへ`click()`を委譲するだけなので、新しいタブは
  開かず同じタブ内で遷移する。ホームに既にいる時に押すと、既存の
  「ホームアイコンクリックで更新」の仕組みがそのまま働くので、自然に
  「更新」としても使える。
- **「未読のみ表示」「動画のみ表示」で、5列目でD／1列目でA を押しても次の行へ折り返さない不具合を修正**：
  前回入れた`spatialNeighbor()`（実測位置ベースの移動）は「指定方向に
  厳密にあるタイル」だけを候補にしていたため、行の右端／左端では次の行へ
  折り返す候補が見つからず動けなくなっていた。A/D（左右移動）は表示/非表示に
  関わらず配列の並び順（＝読み順）自体は変わらないことを利用し、単純に
  配列インデックスを+1/-1しながら次の表示中タイルを探す方式に戻した
  （これだけで行の折り返しも自然に実現できる）。W/S（上下移動）は
  引き続き実測位置ベースのまま。
- **「未読のみ表示」「動画のみ表示」で、Spaceやマウススクロール時に選択がほぼランダムなタイルへ飛ぶ不具合を修正**：
  `updateSelectionFromScroll()`（マウススクロール／Spaceの滑らかスクロールで
  選択位置を追従させる処理）が、フィルタで隙間なく詰めている時も
  相変わらず配列インデックス(`i % cols`)で「同じ列」を判定していたため、
  実際の見た目の列とズレて別のタイルに飛んでいた（ユーザーの推測通り
  「非表示にしているだけで存在はしている」ことが原因だった）。フィルタが
  有効な間は列を保つ判定自体をやめ、表示中タイルの中から中央に一番近い
  ものを探すだけにした。
- **TL/メディア読み込み後、正しく左上から始まったのに数秒後もう一度左上へリセットされる不具合を修正**：
  原因が判明：初期読み込み(`pumpMore`)の完了を待つ数秒の間にユーザーが
  既にWASDで操作を始めていても、完了時のコールバックが無条件に
  `selIndex`を0へ戻し、`settleGridOrder()`でタイルの並び替えまで行って
  いた（並び替えは配列そのものをソートするため、既に操作済みのselIndexが
  指す中身まで変わってしまう問題もあった）。既に操作済み
  (`maxSeenIndex > 0`)ならこの「揃え直し」処理全体をスキップするように
  修正（ホーム更新時の同種の処理にも同じ修正を適用）。
- **右上の検索ボックス表示について**：v3.29.0で実装済み。`hideSidebar`設定
  （デフォルトOFF）がOFFの間は、DOM要素を一切移動させずXの本物の
  サイドバーをそのまま表示するため、通常のTLと全く同じ右上の位置に
  検索ボックス（予測候補込み）が出る。もし出ていなければ設定ページ
  （ツールバー右側の⚙設定、またはbrave://extensionsの詳細）で
  「グリッド表示中は右サイドバーを隠す」がOFFになっているか確認してほしい。

## v3.31.0：メディア欄の画像/動画フィルタの誤解を訂正・「画像」「動画」ピルを両方常設・未読/動画のみ表示を隙間なく詰める

v3.30.0で「メディア欄に動画も表示」のつもりで入れた変更が実は誤りだったと実機報告で判明し、修正した回。

- **`?filter=photo`を外したら「画像の隣に動画も」ではなく「画像が消えて動画だけ」になっていたのを修正**：
  実機で確認し直した結果、Xの`/username/media`は**フィルタ無し(bare)=動画のみ、
  `?filter=photo`=画像のみ**という、常にどちらか片方しか返さない仕様だった
  （v3.29.0時点の「n件の画像と動画」という見出しテキストを鵜呑みにしたのが
  誤りの原因で、実際のセルの中身を検証していなかった）。画像をデフォルトに
  してほしいというのが最初からの要望だったため、`?filter=photo`への
  自動リダイレクトを復活させた。
- **「画像」「動画」の切り替えピルを常に両方表示するように変更**：Xの
  「メディア」タブ自体は実機確認で「画像」⇔「動画」を動的に切り替える
  単一の可変タブ（常に片方しか出ない）だったため、そのタブ枠は使わず、
  自分で「画像」（`?filter=photo`）と「動画」（bare）の2つのリンクを
  組み立てて常設した。「動画」ピルをクリックした直後だけ、上記の自動
  リダイレクトが即座に画像へ引き戻してしまわないよう1回だけ抑制する
  仕組みを追加。
- **同じ「メディア」モードのまま画像⇔動画を切り替えても中身が更新されない不具合を修正**：
  `activateGrid(mode)`の早期return判定が`mode`文字列（'media'等）だけを
  見ていたため、画像/動画どちらも同じ'media'のまま切り替わらず、2回目以降の
  切り替えが無視されて古い内容が表示され続けていた。`location.href`も
  合わせて比較するように修正（別アカウントのメディア欄へ移動した時に
  同様の問題が起きる可能性があった分も合わせて解消）。
- **メディア欄では「動画のみ表示」トグルを非表示に**：`?filter=photo`側では
  動画のタイル自体が存在しないため、押しても意味が無い（このボタンは
  いいね／ブックマーク／ホームなど、写真と動画が混在し得るページでのみ表示）。
- **「未読のみ表示」「動画のみ表示」で隙間だらけになる不具合を修正**：
  v3.30.0では対象外タイルをopacity:0にするだけで位置を動かさない方式に
  していたため、実機報告の通り隙間だらけになっていた。display:noneで
  隙間なく詰めるよう変更。ただし配列インデックス(`i % cols`)でWASDの列を
  計算する既存方式のまま詰めると、CSS Gridの自動配置により見た目の列と
  インデックスの対応が崩れてWASDが壊れる（過去に同じ理由で繰り返し起きた
  事故）。そのためフィルタが有効な間だけ、選択中タイルの実際の描画位置から
  「指定方向に一番近い、表示中のタイル」を探す方式(`spatialNeighbor()`。
  `updateSelectionFromScroll()`と同じ「実測位置で判断する」考え方)に切り替え、
  フィルタが無い時の従来の配列インデックス計算には一切手を触れていない。

## v3.30.0：単独表示の拡充・偽の検索ボタン撤去・未読/動画のみ表示トグル・「•••」メニューのキー操作化 ほか

- **単体/複数/単体と並んだ投稿をDで送る時、複数画像の投稿も1枚ずつ見てから次へ進むように修正**：
  グリッド一覧から画像クリックで開く単独表示(`view`)は、複数画像の投稿でも
  1枚目の画像しか見せずD連打で次の投稿へ飛んでしまっていた。投稿内の
  どの画像を見ているかを`Grid.viewImageIndex`として別途持ち、A/D(delta=±1)
  ではまずその投稿の画像を全部送り切ってから次の投稿へ進むようにした
  （W/Sによる複数投稿の一気送りは従来通り）。複数画像の時は左下に「1/2」
  のような枚数バッジも出す。
- **単独表示中にA/Dで送ると一瞬キャプションが画像の場所にめり込んでちらつく不具合を修正**：
  毎回`body.innerHTML=''`で画像・キャプションを丸ごと作り直していたため、
  新しい`<img>`が読み込まれるまでの一瞬だけ画像の実寸が0になり、隣の
  キャプションパネルが画像の場所へ一瞬めり込んでから正しい位置へ飛んで
  見えていた。既存の`<img>`要素のsrcだけを差し替える方式に変更し、
  ブラウザが新しい画像が届くまで前の画像を表示し続ける挙動を利用して
  ちらつきを無くした。
- **ツールバーの偽の検索ボタン（押すと/exploreへ移動するだけのもの）を撤去**：
  v3.29.0で「サイドバーを隠さなければ本物の検索ボックスがそのまま使える」
  ように直したため、二重に用意する必要が無くなった。むしろ紛らわしいとの
  指摘を受けて撤去し、`hideSidebar`設定の初期値もOFF（サイドバーを隠さず、
  本物の検索がデフォルトで使える）に変更した。
- **「未読のみ表示」「動画のみ表示」のトグルボタンを追加**：グリッドの
  タイル位置は一切動かさず、対象外のタイルをopacity:0で見た目だけ消す
  方式にした（過去にCSSの`order`やDOM挿入位置でグリッドを詰めた際、配列の
  インデックスと見た目の列がズレてWASDが壊れた事故を繰り返しているため、
  今回は位置を一切動かさないことを優先した）。W/A/S/Dは非表示中のタイルを
  自動で飛ばして次の表示中タイルへ移動する。
- **「•••」メニューをキー1つで開けるように変更（設定でリマップ可能なキー）**：
  以前はメニューを開いて自動で「興味がない」をクリックしていたが、その
  項目自体が無いポストもあり説明・挙動として不適切との指摘を受けた。
  キーの役割は「メニューを開く」だけにし、開いている間はW/Sでメニュー内を
  移動・Spaceで選択・Qで閉じるようにした。実機確認：Xの「•••」メニューは
  元からrole="menu"のネイティブなキーボード操作可能ウィジェットとして
  実装されており、合成KeyboardEventでもArrowUp/ArrowDown/Escapeに反応する
  ため、W/S/Q/SpaceをArrowUp/ArrowDown/Escape/Enterへ変換して転送するだけで
  実現できた。

## v3.29.0：列ずれ再発防止・複数画像Spaceで単独表示を復活・自己紹介リンク・サイドバー実表示・ブックマークURL変更対応・いいね/ブックマークのON/OFF・メディア欄に動画も表示

- **マウススクロール／WASDのSキーで選択が1列目に戻る症状の再発防止策を追加**：
  v3.28.1で「同じ列だけを検索対象にする」修正を入れたが、実機報告では
  WASDのS移動だけでもまだ再現した。原因を確実に断つため、`paintSelection()`
  が自分で`scrollIntoView()`を呼んだ直後は、そのスクロールによって発火する
  `scroll`イベントを`updateSelectionFromScroll()`側で一定時間(500ms)無視する
  ガードを追加した（自分で起こしたスクロールに自分で反応して選択がズレる、
  という自己干渉を構造的に防ぐ）。**この修正を試す際は拡張機能の再読み込み
  だけでなく、x.comのタブ自体もリロード（F5）してください**（content script
  は既に開いているタブには自動で再注入されないため）。
- **複数画像の一覧でSpaceを押すと選択中の1枚を単独でさらに大きく表示するように変更**：
  v3.28.0でSpaceの「開く」動作を廃止していたが、複数画像の一覧（既に全部
  フルサイズで並んでいる状態）からさらに1枚を大きく見たい、以前と同じ
  感覚のSpaceが欲しいとの要望を受けて復活させた。新しい表示階層
  `subview`として実装し、Q/Escで一覧（1つ手前）へ戻る。A/Dでの画像切替・
  端まで行った時の隣のツイートへの乗り換えも単独表示のまま続けられる。
- **プロフィールの自己紹介文中のリンクをクリックできるように修正**：
  自己紹介文を`.textContent.trim()`で平文化していたため、URL/ハッシュタグ/
  メンション等の本物の`<a>`タグごと消えてクリックできないプレーンテキスト
  になっていた（実機報告）。子ノードを辿って`<a>`とプレーンテキストを
  区別し、クリックできるリンクとして組み立て直すようにした。
- **サイドバー（検索ボックス等）が実際には見えていなかった問題を修正**：
  v3.28.0で導入した「検索ボックスの入っているウィジェットだけ残す」方式は、
  実はグリッド本体(`.xmr-shell`)がposition:fixed; right:0でナビ右端から
  ビューポート右端まで不透明に覆う作りだったため、隠さず残しても常に
  shellの下敷きになって見えなくなっていた（実機報告「検索ボックスどうした」
  で発覚）。設定「グリッド表示中は右サイドバーを隠す」がOFFの時は、shellの
  右端をサイドバーの左端で止めて覆わないようにし、Xの本物のサイドバー
  （検索の予測候補も含む）がそのまま普通に見える・使えるようにした。ONの
  時は今まで通りサイドバーを丸ごと隠して横幅をグリッドに使う。
- **ブックマークのURL仕様変更に対応**：Xの仕様変更で`/i/bookmarks`へ
  アクセスすると`/i/history`（「ブックマーク」がデフォルト選択された
  「いいね」とのタブ切替UI）へ自動リダイレクトされるようになっていた
  （実機確認）。旧URLしか見ていなかったため、ブックマークページ自体が
  検出されず「画像のみ表示」トグルごと出ない状態になっていた
  （実機報告）。新URLも対象に追加。
- **いいね欄にも「画像のみ表示」のON/OFF切り替えを追加**：今まではいいね欄
  だけ常時ON固定だったが、ブックマーク／ホームと同じ仕組みに揃えた。
- **メディア欄で画像の隣に動画も表示されるように変更**：`?filter=photo`は
  Xの本物のサーバー側フィルタで、動画の投稿を結果から完全に除外して
  しまうことが実機確認で判明した（クエリを付けずに開くと見出しが「n件の
  画像と動画」になり動画も含めて返ってくる）。動画の投稿も
  `[data-testid="tweetPhoto"] img`（中身は動画のサムネイル画像）を持って
  いるため、このフィルタさえ外せば既存の写真収集ロジックがそのまま動画も
  拾える。フィルタの自動付与を撤回し、タイルには本物同様の再生アイコンの
  バッジを付けた（実際の再生はできず静止画のサムネイルのまま。動画を
  見るには「このポストを開く（R）」でXの本物のページへ）。

## v3.28.1：v3.28.0の不具合4件を修正（列ずれ・Space時のスクロール漏れ・複数画像のキャプション欠落）

v3.28.0のマウススクロール追従・複数画像フルサイズ表示・Space変更を実装した直後に見つかった不具合をまとめて修正。

- **マウスでスクロールすると必ず1列目に戻る／WASDのSでも1列目に戻される不具合を修正**：
  `updateSelectionFromScroll()`が「シェル中央に一番近いタイル」を縦距離だけで
  選んでいたため、同じ行のタイルは中央からの縦距離が全く同点になり、常に
  配列内で先頭（＝1列目）が選ばれていた。WASDのSキーで正しい列に移動した
  直後も、その移動が起こす`scrollIntoView()`のスクロールで同じ処理が再度
  走り、1列目へ戻されていた。現在の列（横位置）は固定したまま縦方向だけ
  スクロール位置に追従するよう修正。
- **複数画像を開いている時にSpaceを押すと背後のページがスクロールしてしまう不具合を修正**：
  Spaceキーの処理がグリッド一覧(`grid`)の時しか`preventDefault()`していな
  かったため、サブグリッド（複数画像）やビューア（単一画像拡大）表示中に
  Spaceを押すとブラウザ標準の「ページを1画面下へスクロール」動作がそのまま
  発火していた。サブグリッド／ビューア表示中もpreventDefaultして、選択画像
  はそのまま・何も起きないようにした。
- **複数画像を開いた時だけ投稿者名や「このポストを開く」リンクが出ない不具合を修正**：
  単一画像用の`renderViewer()`にはアイコン・投稿者名・本文・「このポストを
  開く（R）」「このアカウントのメディアを開く（F）」リンクを表示するキャプ
  ションパネルがあったが、複数画像用の`renderSubgrid()`には元から実装され
  ておらず、画像だけが並ぶ形になっていた。キャプション部分を共通関数
  `buildViewerCaption()`に切り出し、両方の画面で使うようにした（機能自体
  ＝R/Fキーでのタブオープンは元々`currentEntry()`経由で動いていたが、画面上
  にリンクが見えていなかった）。

## v3.28.0：サイドバーの検索ボックスを元の位置に残す・複数画像を開いた瞬間からフルサイズ表示・Spaceキーを滑らかスクロールに変更・マウススクロールに選択位置を追従

- **右サイドバーの検索ボックスだけを元の位置に残す方式に変更**：
  「見た目は検索ボックスのボタンで/exploreへ移動する」方式について、
  「サイドバーのあの検索ボックスをそのまま持ってくればいいのでは」と
  提案を受けた。まずDOM要素を移動させる方式を実機で試したところ、
  移動させると内部の予測候補パネルの位置決めが壊れる（虫眼鏡アイコンも
  プレースホルダーも消えた見た目になった）ことを確認したため、代わりに
  **検索ボックスを含むウィジェットだけを元の位置に残し、その兄弟の
  他のウィジェット（トレンド・おすすめユーザー等）だけを個別に隠す**
  方式に変更した。実機で予測候補・入力の両方が正しく動くことを確認した
  （`hideSidebarExceptSearch()`）。ツールバーの検索ボタンは撤去し、
  サイドバーの本物の検索ボックスをそのまま使ってもらう。
- **複数画像のツイートを開いた瞬間から全部フルサイズで並べるように変更**：
  以前はサムネイル一覧→クリックで1枚拡大、という二段階だったが、開いた
  瞬間からフルサイズで並べてほしいとの要望を受けて一段階にまとめた
  （`renderSubgrid()`が`mediumSrc`ではなく`fullSrc`を使うようになり、
  クリックしても「選択が変わるだけ」でそれ以上開かなくなった）。これに
  伴い、複数画像を「サムネイル一覧→単一拡大」の二段階で扱っていた
  内部状態（`Grid.viewerParentLevel`等）が丸ごと不要になったため、
  関連コードを整理・削除した。
- **Spaceキーの動作を変更**：グリッド一覧で「開く」動作をしていたが、
  Xの通常のタイムラインのように1画面分くらい滑らかにスクロールする
  動作にしてほしいとの要望。開く操作はQに一本化した（元々Qでも開けて
  いた）。
- **マウスでスクロールした後の選択位置のズレを解消**：スクロールしても
  選択位置(青い枠線)が追従しないため、その後WASDに切り替えると
  `scrollIntoView()`に引っ張られて元の位置へ急に戻ってしまう、という
  報告があった。スクロールのたびに「今画面中央に一番近いタイル」へ
  選択位置を追従させるようにした（`updateSelectionFromScroll()`、
  rAFで間引いて負荷を抑える）。

## v3.27.0：検索ボックスから本物の検索候補（予測）を使えるように

v3.26.1で検索を入力ボックスに戻したが、「Xの本物の検索にあるような予測
候補（最近の検索・トレンド・候補アカウント）が無い」との指摘があった。
これを自作しようとすると、Xの内部API（GraphQL）を直接叩く必要があり、
壊れやすく複雑になる（＝過去に何度も苦労した「偽物のサジェスト」問題に
逆戻りするリスクが高い）。

実機で確認したところ、左ナビの「話題を検索」
(`[data-testid="AppTabBar_Explore_Link"]`)を押すと`/explore`へ遷移し、
そこに本物の検索候補が出る入力欄
(`[data-testid="SearchBox_Search_Input"]`)があった。そこで自作の入力欄は
やめ、**見た目は検索ボックスのボタンだが、押すと本物の検索ページへ
移動し、本物の検索入力欄へ自動でフォーカスする**方式にした。フォーカス
した瞬間に「最近の検索」等の本物の候補がそのまま出る（Xの実物の機能を
そのまま使ってもらう、フォローボタンと同じ考え方）。

## v3.26.1：検索を再び入力ボックスに戻す

v3.13.0で「/exploreへのリンク」に変えていたが、検索ボックスに戻して
ほしいとの要望。以前の自作ボックスが壊れて見えた原因は、サジェスト機能を
持たない偽物なのに候補欄がある体裁にしていたことだったので、今回は
サジェスト欄を一切作らず、Enterで素直にXの検索結果ページ
（`x.com/search?q=...`）へ遷移するだけのシンプルな入力欄にした
（見せかけの機能を持たせなければ「壊れて見える」余地が無い）。

## v3.26.0：【最重要バグ修正】投稿が丸ごと欠落するバグの根本原因を特定・修正

ユーザーが「画像のみ表示をOFFにして手でゆっくり全部スクロールしてから
ONにすると正しく出るが、普通にリロードすると投稿が2件丸ごと消えて
いきなり数ヶ月前の投稿に飛ぶ」という決定的な比較を示してくれたことで、
これまでで最も明確に原因を特定できた。

- **【根本原因】`pumpMore()`が読み込みを進めるために
  `window.scrollTo(0, document.documentElement.scrollHeight)`で**一気に
  最下部までジャンプ**していた**。Xの仮想リストは、スクロールで実際に
  通過しなかった範囲のセルをそもそもマウントしない（＝該当の投稿を
  一切描画しない）ことがある。そのため、大ジャンプで読み込みを進めると
  「跳び越えた範囲の投稿が丸ごと欠落する」という、単なる並び順の乱れ
  よりも深刻な問題が起きていた。ユーザーが手でゆっくりスクロールした
  時は全ての投稿を実際に通過するため正しく全部拾えていた、という説明と
  完全に一致する。
- **対策**：`pumpMore()`を、最下部への大ジャンプではなく、画面の約80%分
  ずつ`window.scrollBy()`で少しずつ進める方式に変更した。実際にゆっくり
  スクロールするのと同じように、Xの仮想リストが範囲内の全セルを確実に
  マウントするようにする。読み込み対象件数に届くまでの試行回数の上限も
  合わせて引き上げた（大ジャンプより1回あたりの進む距離が短くなるため）。

## v3.25.0：並び順の正しさとWASD操作の安定性、両立できないトレードオフの正体を特定・設計変更

v3.24.1でDOM挿入方式に戻したにもかかわらず、「Sキーを押しているだけで列が
ずれる」という報告が続いた。ユーザーからの再現性の高い説明（「画像ごとに
増えるたびに列がずれる」）で、真因はDOM挿入とCSS orderのどちらの方式かでは
なく、**そもそも配列の途中へ新しいエントリを挿入すること自体**が、
ユーザーが操作している最中に既存タイルの位置をずらしてしまう、という
構造的な問題だと判明した。

- **並び順の正しさとナビゲーションの安定性を分離**：通常の追加読み込み
  （`harvestNew`が新しいセルを見つけた時）は常に**末尾に追加するだけ**に
  戻した（`appendEntry`、旧`insertEntrySorted`）。これにより、既に画面に
  出ているタイルの位置は絶対に動かなくなる。
- **並び順の補正は、初回表示・更新直後の一括読み込みが完了した1回だけ**、
  `settleGridOrder()`でtranslateY順にソートし直す。まだユーザーが操作を
  始めていないタイミングでの並び替えなので、実害が無い。既存のタイル
  DOM要素はそのまま再利用して位置だけ動かす（作り直さない）。
- 選択位置(`Grid.selIndex`)・既読の区切り(`Grid.maxSeenIndex`)の補正ロジック
  （挿入位置に応じて++する処理）も、常に末尾追加になったことで不要になり
  撤去した。
- ライトボックスの「Xで開く」ボタンから「いいね・リプライ等はこちらで」の
  文言を削除（不要との指摘）。
- メディア欄がまだ直っていない件を受けて、`chrome.debugger`経由の擬似
  スクロールにデバッグログを追加した（`brave://extensions`→本拡張の
  「service worker」リンクから確認可能）。次回はこのログを見て、
  `debugger.attach`自体が失敗しているのか、送信はできているが効果が
  無いのかを切り分ける。

## v3.24.1：【重大バグ修正】WASD移動が崩壊していたのを撤回・更新後の選択ハイライト漏れを修正・拡大表示のボタン改善

- **【最重要・重大バグ修正】v3.22.0のCSS order化がWASD移動を壊していた**：
  「Sキーで下に移動しているつもりが横にずれていく」という報告があった。
  原因は、WASD移動(`moveRow`→`step`)がGrid.entries配列のインデックス計算
  だけで「何行下か」を決めているのに対し、CSSのorderプロパティで見た目の
  並び順だけを変える方式にしたことで「配列の並び順＝実際に画面上の並び順」
  という前提が崩れてしまったこと。正しさを最優先し、DOM上の挿入位置を
  配列のインデックスと確実に一致させる（`insertBefore`）方式に戻した
  （ガクつき対策は撤回。並び順の正しさの方が優先度が高いと判断）。
- **更新後、選択ハイライト（青い枠線）が表示されないことがあるバグを修正**：
  更新直後は裏のリストがまだ空/スケルトンで、その時点の`harvestNew()`では
  0件のことがあり、`paintSelection()`が何もハイライトできないまま
  終わっていた。`pumpMore()`で本文が揃った後にもう一度`paintSelection()`を
  呼ぶようにした。
- **拡大表示（ライトボックス）のボタンを改善**：「Xで開く（いいね・
  リプライ等はこちらで）」を「このポストを開く（R）　いいね・リプライ等は
  こちらで」に変更し、新たに「このアカウントのメディアを開く（F）」ボタンも
  追加した。

## v3.24.0：chrome.debugger経由の擬似スクロールで自動修復を試みる（要・新しい権限）

v3.23.0で「window.scrollTo()では直せない、本物のマウスホイール操作なら
直る」ことを実機で確認したが、content scriptからは本物の（信頼できる）
入力イベントを生成できないため自動化できない、という結論だった。ユーザーに
相談したところ、より自動化に近い方法を試すことになったため実装した。

- **【重要：新しい権限】`manifest.json`に`"debugger"`権限を追加した**。
  `chrome.debugger`（Chrome DevTools Protocol）経由で`Input.dispatchMouseEvent`
  を送り、本物に近いマウスホイール入力をシミュレートする。
  `window.scrollTo(0,0)`のリトライで直らない場合、background.js経由で
  ホイールイベントを6回ほど連続で送るようにした（実機報告：1回の
  スクロール操作だけでは全部は反映されないことがあったため）。
- **注意点（重要）**：`chrome.debugger`でタブにアタッチしている間、
  ブラウザ上部に「拡張機能がこのブラウザをデバッグしています」という
  警告バーが一時的に表示される。できるだけ短時間（1秒未満）で完了する
  ようにしているが、この見た目の変化はユーザーに事前説明済み。
  `window.scrollTo()`のリトライ・案内メッセージ表示も保険として残して
  あるので、`chrome.debugger`が何らかの理由で使えない/効かない場合でも
  完全に機能停止することはない。

## v3.23.0：メディア欄の並び順問題の「真の」根本原因を特定（プラットフォーム側の制約）・案内メッセージを追加

Claude-in-Chromeの`computer`ツール（実際のマウスホイール操作をエミュレート
できる）を使い、これまでで最も踏み込んだ実機調査を行った。結果、確定的な
結論に至った。

- **【確定】`window.scrollTo(0,0)`をJavaScriptから何度呼んでも、Xの仮想
  リストが確実に再同期するとは限らない**：実機で`window.scrollY`は0に
  なるのに、最初のセルの`translateY`は5000px超のまま変わらないケースを
  確認した。位置を変えながら複数回`scrollTo()`を呼ぶ（＝scrollイベント
  自体は確実に複数回発火させる）方式も試したが効果が無かった。一方、
  `computer`ツールで**本物のマウスホイール操作**をエミュレートしたところ、
  即座に直った。
- **これはコードのバグではなく、ブラウザの仕組み上の制約**：content
  scriptは信頼できる（trusted）ユーザー入力イベントを生成できない
  （セキュリティ上の制約）。Xの仮想リスト実装が本物の入力イベントでしか
  再同期しないタイプのものである場合、content scriptからは原理的に
  完全な自動修復ができない。
- **対策**：自動でのリトライは今まで通り試みるが、それでも直らない場合は
  「マウスホイールを一度スクロールしてみてください」という案内を表示する
  ようにした（`activateGrid()`と`refreshHomeTimeline()`の両方）。実機で
  確認した通り、ユーザーが一度スクロールすれば直る。

## v3.22.0：キー割り当てのリマップ機能を実装・並び替えをCSS orderに変更（ガクつき対策）・設定の即時反映バグ修正

- **【新機能】キー割り当てのリマップ機能を実装**：以前「まずは表示設定と
  一覧のみ、リマップは見送り」としていたが、要望を受けて実装した。
  W/A/S/D・矢印・Q・Space・Escは移動と開閉の核となる操作なのでリマップ
  対象外とし、それ以外の単発アクションキー（そのツイートを開く／投稿者の
  メディア欄を開く／いいね／ブックマーク／ホーム更新／プロフィール→
  メディア欄／興味がない）を設定ページの入力欄からリマップできるように
  した。1文字のみ・重複不可のバリデーション付き。`content.js`側は
  `Settings.keys.xxx`を参照するように全キー判定を書き換えた。
- **並び替えの実装をDOM移動からCSSの`order`プロパティに変更**：前バージョンで
  translateY基準の挿入位置修正を実装したが、`insertBefore()`でDOMノードを
  実際に動かす方式だったため、下の方までスクロールして追加読み込みが
  起きるたびにグリッド全体がガクつく（挿入位置より後ろの全タイルが
  レイアウトし直しになる）という新たな問題が起きていた。DOM上は常に
  末尾へ`appendChild`するだけにして、見た目の並び順はCSSの`order`
  プロパティ（`tile.style.order = translateY`）に任せる方式に変更。
  正しさは保ったまま、ガクつきを解消した。
- **【重大バグ修正】設定ページで「右サイドバーを隠す」をOFFにしても、既に
  表示中のグリッドには反映されず、次にグリッドを開き直すまでサイドバーが
  隠れたままだった**。`chrome.storage.onChanged`のリスナーで、グリッドが
  今まさに表示中ならその場でサイドバーの表示/非表示を切り替えるよう修正。
- プロフィールのメディア欄カードのアイコンをクリックしたら、そのアカウントの
  メインメニュー（ポスト欄）へ移動するように（前バージョンから継続）。
- フォローボタンの色をXの本物と同じ配色に（前バージョンから継続）。

なお、メディア欄の並び順について「TLは良くなったがメディアはまだ変」との
報告があった。今回のCSS order化は正しさ自体は変えていないので、引き続き
実機確認が必要。

## v3.21.0：並び順が崩れる根本原因を特定・修正（実機でユーザー自身がON/OFF比較して実証）・プロフィールカード改善

ユーザーが「拡張機能ON/OFFで並び順を比較する」というテストを実際に行い、
グリッドの上位5件と、ネイティブページを手動で一番上までスクロールした
時の上位5件が完全に別のアカウントであることをスクリーンショットで実証
してくれた。これを受けて`harvestNew()`の実装を再点検し、根本原因を特定した。

- **【根本原因】`harvestNew()`は見つかった順にただ`push`/`appendChild`して
  いた**。テキストのみのセル（画像が無いツイート）は「本当に画像が無いか」
  を確定するまで最大`photoPendingMs`(4秒)待ってから追加される仕組みに
  なっているが、画像付きのセルは待たずにすぐ追加される。そのため、
  画面上ではより上にあるテキストのツイートが、それより下にある画像付き
  ツイート（すぐ確定する）より後からグリッドに追加されることがあり、
  ネイティブの並び順とグリッドの並び順がズレていた。
- **対策**：セルの`translateY`（画面上の実際の縦位置）を基準に、二分探索で
  正しい位置に挿入する方式に書き直した（`insertEntrySorted()`）。処理が
  完了した順ではなく、常に画面上の「上から下」の順番を保つ。挿入位置が
  現在の選択位置や既読の区切りより前だった場合は、それらのインデックスも
  ズレないよう補正する。
- **メディア欄のプロフィールカードのアイコンをクリックしたら、そのアカウントの
  メインメニュー（ポスト欄）へ新しいタブで移動するように**。
- **フォローボタンの色を、Xの本物と同じように変更**：「フォロー」は白のまま、
  「フォロー中」になったら黒背景・白文字・枠線にする。

## v3.20.1：タイルアクションボタンを撤回・全画面読み込みオーバーレイを撤回・重大バグ2件を修正

v3.20.0で追加したタイルアクションボタンは「押してもいいねが押せているわけ
ではない」「邪魔」との指摘で完全に撤回した。帯クリックで新しいタブへ飛ぶ
挙動も「邪魔」との指摘で撤去（帯クリックは通常のタイル選択にフォールバック
するだけになった）。

- **タイルアクションボタン（♡🔖↗✕）を完全撤回**（content.js/content.css/
  options.html/options.jsから削除）。
- **帯クリックで新しいタブへ飛ぶ動作を撤去**。
- **既読タイルの帯の色をもっとはっきり変更**（`#182430`→`#1c3a56`、薄すぎて
  分からないとの指摘）。
- **メディア欄（プロフィールの/media）にも画像のみ表示ON/OFFを追加**。
  OFFの時は`?filter=photo`への自動リダイレクトはそのまま残し、グリッド化
  だけしない（＝普段通りのXの表示）。
- **【重大バグ修正】v3.19.1で追加した「読み込み中」の全画面オーバーレイが、
  Xのナビ全体（検索アイコン等）まで隠してしまい、「検索ボックスが消えた」
  「読み込み中で止まった（＝壊れたように見える）」という新たな不満の原因に
  なっていた**。画面全体を覆う代わりに、ナビ操作を一切邪魔しない右下の
  小さいバッジだけに変更した。
- **【重大バグ修正】`document.body`がまだ存在しないタイミング
  （`document_start`直後、/homeへの直接ロード等）で`activateGrid()`が
  `document.body.appendChild()`を呼んで例外を起こし、`async`関数の中の
  例外は呼び出し元へ伝播せず静かに失敗したPromiseになるだけなので、
  以後誰も再試行しないままグリッドが有効化されない、というバグを修正**。
  これが「画像のみ表示はONのはずなのに、開いた直後はOFFの見た目で、
  手動でOFF→ONと切り替えて初めて出てくる」の原因だった。
  `ensureHomeToggle()`に元々あった「bodyが無ければ待つ」という対策と
  同じものを`activateGrid()`にも追加した。
- **ホーム更新（`refreshHomeTimeline()`）にも、初回ロード（`activateGrid()`）
  と同じスクロール位置の検証ロジックを追加**：今までは固定2秒待つだけで
  `resetGridEntries()`を呼んでいたため、Fキーの件と同じ原因（本文の無い
  スケルトンで判定してしまう／Xのスクロール位置復元が後から効く）で、
  更新後も途中のツイートから表示されてしまうことがあった。本文入りの
  セルでtranslateYが小さいことを確認してから進めるよう統一した。
- なお、DOM上の並び順が画面上の並び順（translateY）と一致しているかも
  実機で確認したが、現時点のスナップショットでは一致していた（この経路の
  バグではなさそう）。

## v3.20.0：タイルに小さいアクションボタンを追加・選択中の画像にも枠線

- **タイルに小さいアクションボタン列（♡ いいね／🔖 ブックマーク／↗ 新しい
  タブで開く／✕ 興味がない）を追加**：もう一つのプロジェクト
  （x-taste-digest）のタイルUIを参考にしたいという要望を受けて追加。
  画像タイルは画像下端に重ねるオーバーレイ、テキストタイルは帯の先頭に
  通常のボタン列として配置。押すと、選択中かどうかに関わらずそのタイルの
  ツイートに対して2/3/R/Xキーと同じ処理を直接実行する。options.htmlの
  設定でON/OFFを切り替えられる。
- **選択中のタイル／通常タイムライン選択で、画像自体にも枠線が付くように**：
  今まではタイル全体・セル全体の枠線だけで、画像そのものには付いていない
  ように見えるとの指摘があったため、画像にも同じ色の枠線を追加した。

## v3.19.1：拡張機能導入後にXが完全に固まる重大バグを修正・Fキーの表示位置バグを実機調査で特定・修正

- **【最重要・重大バグ修正】拡張機能導入直後にXが完全に固まる（クラッシュ
  したように見える）バグを修正**：v3.19.0で追加した「画像のみ表示」トグル
  ボタンのラベル更新処理が、値が変わっていなくても無条件に`textContent`へ
  書き込んでいた。これがdocument.body全体を監視している`bodyObserver`
  （`childList: true, subtree: true`）を発火させ、その中でまた同じラベル
  更新処理が呼ばれて…という**MutationObserverの無限フィードバックループ**
  になっていた。ホーム／ブックマークのページを開いた瞬間にトグルボタンが
  作られるため、ほぼ確実にこの無限ループへ突入し、タブのJSスレッドを
  完全に専有してブラウザごと固まったように見えていた。値が実際に変わる
  時だけ書き込むよう修正。**今後、document.body全体を監視するobserverの
  コールバック（またはそこから間接的に呼ばれる関数）の中でDOM書き込みを
  行う時は、書き込み前に値の変化を確認することを徹底する。**
- **【重大バグ修正】Fキーでプロフィールのメディア欄を新しいタブで開いた時、
  最初は1件だけ・まだ途中から表示されている、何度かタブを開き直して
  ようやく正しく表示される、という長年報告されていたバグの原因を実機で
  特定・修正**：Claude-in-Chromeで実際にこの操作を再現し、`window.scrollY`
  が7000px超、最初のセルの`translateY`も5000px超という「途中」の状態を
  確認した。原因は2つの組み合わせ：①Xは同じアカウント・同じページへの
  再訪問時、以前のスクロール位置を内部的に記憶していて復元することがある。
  ②先頭判定のリトライループが「cellInnerDivが存在してtranslateYが小さい」
  ことだけを条件にしていたが、本文（`<article>`）がまだ描画されていない
  空のスケルトン状態のセルはtranslateYが0付近になりやすく、それだけで
  「先頭に戻れた」と誤判定して早期にループを抜けていた。本当の本文が
  その後（実機確認では最大20秒ほど）描画される時にXが①のスクロール位置
  復元を行っても、ループは既に終わっているため気付けなかった。対策：
  本文を実際に持つセルが現れるまでは先頭判定をしないようにし、待てる
  時間も大幅に延ばした。あわせて、この待ち時間中に何も表示されず固まって
  見えないよう簡易な読み込み表示（`.xmr-early-loading`）を追加。
- **設定ページへの導線をツールバーに追加**：`brave://extensions`の詳細
  からしか開けず分かりにくいという指摘があったため、ツールバーに「⚙設定」
  ボタンを追加した。`chrome.runtime.openOptionsPage()`はcontent script
  から直接呼べないため、`background.js`にメッセージで開くよう依頼する
  仕組みにした。
- **Xキー（「興味がない」）の説明を設定ページに追記**：「•••」メニューを
  開いて自動選択する仕組みであることを明記（Escでメニューを閉じる操作には
  今のところ非対応、という注記も追加）。
- **画像のみ表示トグルの「（実験的）」表記を削除**。
- **トグルボタンの位置を実測方式に変更**：固定90pxに直しても「まだ変」との
  指摘があったため、ズーム倍率やウィンドウサイズで変わるアカウント
  アイコン行の実際の位置を毎回`getBoundingClientRect()`で測り、その少し
  上に配置するようにした（このプロジェクトで何度も効果があった「Xの
  レイアウトに頼らず自分で実測する」方式をここにも適用）。

## v3.19.0：R/F入れ替え・タブを閉じたら元のタブへ戻る・タブ毎の画像のみ表示記憶・設定ページ新設

このバージョンから初めて**content script以外の構成要素**（background service
worker、設定ページ）が追加された。今までは「x.comのページ内で動くJSだけ」
という構成だったが、タブのフォーカス制御はページ内のJSからは行えない領域
（ブラウザのタブ管理そのもの）のため、拡張機能のbackground側の力が必要に
なった。

- **R/Fキーを入れ替え**：R＝そのツイートを新しいタブで開く、F＝投稿者の
  メディア欄を新しいタブで開く（前バージョンの逆）。
- **【新機能】R/Fで開いた新しいタブを閉じたら、必ず元のタブへ戻るように**：
  ブラウザの既定動作だと、閉じたタブの隣（多くの場合右側）にフォーカスが
  移ってしまい、複数タブを開いた後に「元の場所に戻りたい」のに戻れない、
  という報告があった。`background.js`という新しいファイルを追加し、
  `chrome.tabs.onCreated`/`onRemoved`で「どのタブがどのタブから開かれたか」
  を記録し、子タブが閉じられたら開いた側のタブへ`chrome.tabs.update({active:
  true})`で強制的にフォーカスを戻すようにした。x.com/twitter.comを開いていた
  タブから開かれたタブだけを対象にしている（ブラウザ全体のタブ挙動を変える
  ものではない）。`manifest.json`に`"permissions":["tabs"]`と
  `"background":{"service_worker":"background.js"}`を追加。
- **画像のみ表示のON/OFFを、ホームのタブ（おすすめ／フォロー中／自分で作った
  リスト）ごとに、さらにブックマークにも個別に記憶するように変更**：
  以前はホーム全体で1つのON/OFFしか無く、「おすすめ」以外は強制OFFに
  していたが（v3.15.0〜）、タブ毎に別々に覚えて、それぞれでグリッド表示を
  使えるようにしてほしいという要望を受けて作り直した。`localStorage`の
  キーを`xmr-image-only:home:おすすめ`のように「どこの設定か」を表す
  文字列(scope)ごとに分けて保存する。ブックマークにも同じ仕組みでON/OFF
  トグルを追加した（以前は常時ON固定でトグル自体が無かった）。
- **画像のみ表示トグルボタンの位置を修正**：左下12pxの固定位置だと、自分の
  アカウントアイコン・ハンドル名の行と重なっていた（実機報告）。その行の
  上まで持ち上げた。
- **【新機能】設定ページ（`options.html`）を新設**：グリッド表示中に右
  サイドバーを隠すかどうかをON/OFFできるようにした。`manifest.json`に
  `"permissions":["storage"]`と`"options_page":"options.html"`を追加。
  content script側とoptionsページは別オリジン扱いでlocalStorageを共有
  できないため、拡張機能全体で共有できる`chrome.storage.local`を使い、
  `chrome.storage.onChanged`で設定変更をリロード無しで即座に反映するように
  した。あわせて、現在使えるキー操作の一覧も同じページに掲載した
  （キー割り当ての変更＝リマップ機能は今回は見送り、次回以降に作り込む
  予定であることをページ内にも明記した）。

## v3.18.0：ホーム更新でグリッドが空になる重大バグを修正・既読の過剰マーク修正・F/2/3/Cキー追加

実機（Claude-in-Chromeで実際にログイン中のブラウザに接続）で調査し、
「更新してもTLの一番上から表示されない／更新した分があまり見えない」という
報告の実際の原因を特定した。

- **【重大バグ修正】ホーム更新後、グリッドが0件になる**：`resetGridEntries()`
  は裏の本物のリスト(`Grid.sourceRoot`)から要素を拾い直す際、`harvestNew()`
  が「処理済み」の目印として各要素に付けている`dataset.xmrDone`をリセット
  していなかった。Xの仮想リストは更新後も同じDOM要素を使い回すことが
  多く（実機確認：更新ボタンを押した直後、裏には9件あるのにグリッドの
  タイルが0件という状態を再現した）、既に`xmrDone`が付いている要素は
  `harvestNew()`に毎回スキップされるため、結果としてグリッドが空っぽの
  ままになっていた。`resetGridEntries()`でこのフラグをクリアしてから
  拾い直すよう修正し、あわせて初回表示と同じ件数まで埋まるよう
  `pumpMore()`も呼ぶようにした。
- **【調査結果】ホームの「おすすめ」タブはそもそも時系列順ではない**：
  実機で先頭5件の投稿IDを調べたところ、3番目の投稿より4番目の投稿の方が
  IDが大きい（＝新しい）のに3番目より後ろに表示されていた。Xの「おすすめ」
  タブは新しい順ではなく関連度・エンゲージメントを加味したアルゴリズム
  順であり、これは仕様（バグでも並び順の反転でもない）。画像のみ表示は
  「おすすめ」タブにしか適用されない設計（v3.15.0〜）なので、常に厳密な
  時系列順にはならない点は仕様として理解してもらう必要がある。
- **既読が実際に見ていないものまで青くなるバグを修正**：`markAllLoadedAsSeen()`
  は「今読み込まれている全部（pumpMoreの先読み分も含む）」を既読にして
  いたため、画面外で先読みされただけの未閲覧タイルまで既読扱いになって
  いた。WASD等で実際に到達した最大位置(`Grid.maxSeenIndex`)を追跡し、
  その範囲だけを既読にするよう変更。
- **既読タイルの見た目を文字色→帯の背景色に変更**：文字を読みにくくする
  という指摘のため、文字は白のまま、帯の背景だけをわずかに変える方式にした。
- **キー割り当て変更・追加**：Enter→**F**（ツイートを新タブで開く。ホーム
  ポジションから遠く押しにくいとの指摘）、**2**でいいね、**3**で
  ブックマーク（実機で`data-testid="like"/"unlike"`,
  `"bookmark"/"removeBookmark"`を確認し、本物のボタンへclick()を委譲する
  方式で実装）、**C**でホーム更新（ホームアイコンとツールバーの更新ボタンは
  同じ`refreshHomeTimeline()`を呼ぶので、これをキーからも呼べるようにした）。

## v3.17.0：既読トラッキングを「表示即既読」から「区切りでスナップショット」方式に作り直し

v3.15.1で画像を暗くしないよう直したが、今度は別の不具合が報告された：
グリッド表示直後、画面内に最初から入っているタイルがその場で全部既読
（青文字）になってしまい、「どこまで読んだか」の目印として機能していなかった。
原因はIntersectionObserverによる「画面に映ったら即既読」という設計そのもの
にあった——初回描画時点で画面内にあるものは、observe()した瞬間に
「intersecting」判定されて即座に既読登録されてしまう。

ユーザーからの提案（「ホームor更新か他のボタンを押したらそこまでを色付ける」）
を採用し、既読登録を継続的な可視性ベースではなく、**明示的な区切りの
タイミングでのスナップショット**方式に作り直した：

- グリッドの`Grid.seenObserver`（IntersectionObserver）を完全に撤去
- 代わりに`markAllLoadedAsSeen()`という関数を新設し、「今読み込んでいる
  ものを丸ごと既読リストに追加する」処理にした
- これを呼ぶタイミングは：他ページへ移動する時（`deactivateGrid()`。いいね/
  ブックマーク/リスト/アカウントタブのリンククリックやホームタブの切替、
  ブラウザの戻る/進むも全部ここを通る）、ホームの更新ボタン
  （`refreshHomeTimeline()`、新着を取り込む前に今の分を既読にする）、
  メディア/いいね/ブックマークの更新ボタン（リロード前）
- 画像を拡大するQキーはどのタイミングにも触れていないので、既読登録の
  トリガーにはならない（ユーザーの要望通り）
- タイル自体の色付け（`.xmr-tile-seen`）は今まで通り、既読リストに既に
  入っているかどうかを見た瞬間に判定するだけなので変更なし

通常タイムライン（グリッド未使用時）側も、IntersectionObserverではなく
「セルの下端が画面上端(0px)より完全に上に出た＝スクロールで通り過ぎた」
という条件に変更した。こちらは更新ボタンに相当するものが無いため、
グリッドのような明示的な区切りではなく「実際に通り過ぎた」ことを区切りに
している（初回表示直後に画面内にあるだけでは既読登録されない）。

## v3.16.0：ホームタブ（おすすめ/フォロー中/自作リスト）をツールバーから切替可能に

v3.15.0で追加した「リスト」リンクが実は`x.com/i/lists`という中身の無い
壊れたページに飛ぶだけだったと判明。本来の要望は「リスト管理ページを
見たい」ではなく「ホームのタブ（おすすめ／フォロー中／ピン留めした自作
リスト）をこのツールバーから切り替えたい」だった。

ホームのタブはURLを持たない（クリックしてもURLが変わらず、実機確認では
`aria-selected`属性だけが付け替わる）ため、プロフィールの`extraTabs`のように
hrefでリンクすることができない。そこで、フォローボタンと同じ「本物の要素は
隠したまま残し、自前ボタンからclick()を委譲する」方式にした。「おすすめ」
以外を押すと、v3.15.0で作った`ensureHomeTabObserver()`が検知して自動で
グリッドを解除し、本物のタイムライン表示に切り替わる（設計上つながっている）。

## v3.15.1：通常ページの既読表示を修正（画像を暗くしないよう限定）・R キー

- **通常タイムラインの既読表示が画像まで暗くしていたバグを修正**：
  v3.15.0では`cellInnerDiv`全体に`opacity`をかけていたため、投稿画像そのもの
  まで暗くなってしまい「絵を見たい」という目的に反する結果になっていた
  （実機報告）。グリッド側の既読表示（`.xmr-tile-seen`）は元々投稿者名・
  本文の文字色だけを変える設計で画像には触れていなかったのに、通常
  タイムライン版だけ誤って全体を暗くしていた。文字（`User-Name`/
  `tweetText`）だけに色を付ける方式に直し、画像には**絶対に**触れないように
  した。
- **投稿者のメディア欄を新しいタブで開くショートカットをShift+Enterから
  Rキー単体に変更**（Enterはそのツイートを開くのみになった）。

## v3.15.0：フォローボタン・リストへの導線・ホームタブ連動・通常ページの既読トラッキング

- **プロフィールのメディア欄カードにフォローボタンを追加**：実機のプロフィール
  ページに接続してdata-testidを確認したところ、`{ユーザーID}-follow` /
  `{ユーザーID}-unfollow`という安定したパターンだった。本物のボタンは
  （非表示にした領域の中に）そのまま残しておき、自前ボタンのクリックで
  そちらへ`click()`を委譲する方式にした（本物のReact管理下のボタンを
  DOM上で移動させるのではなく、隠したまま残す＝過去に見送った「いいね
  ボタンの埋め込み」より安全）。押した後、本物のボタンのラベル変化
  （フォロー→フォロー中）を少し待ってから自前ボタン側にも反映する。
- **ツールバーに「リスト」への導線を追加**（`https://x.com/i/lists`へ）。
  検索・いいね・ブックマーク・更新と同じ並びに追加した。
- **ホームのタブ（おすすめ／フォロー中／自分で作ったリスト）に連動して
  画像のみ表示を自動切り替え**：「おすすめ」タブでは今まで通り画像のみ
  表示のトグル設定に従うが、それ以外のタブ（フォロー中・自作リスト等）
  では自動でOFFにする（＝通常のXの表示に戻す）ようにした。ホームの
  タブ切替はURLが変わらずSPA内の属性変更だけで起きる（実機確認：
  aria-selectedがタブ要素間で付け替わるだけで要素自体は使い回される）
  ため、通常のURL監視では検知できない。タブ一覧のaria-selected変化だけを
  狙う専用のMutationObserverを新設した。
- **通常のタイムライン（グリッド未使用時）でも既読トラッキングに対応**：
  既読データ自体はグリッド用と完全共有（同じlocalStorage）。グリッドの
  既読トラッキングは自前タイルだけが対象だったが、こちらはXの本物の
  cellInnerDivを対象に、画面(viewport)基準で判定する。Xの内部要素の色を
  個別に上書きするとverifiedバッジ等の強調表示と衝突しかねないため、
  内部要素には触れずcellInnerDiv自体をうっすら暗くするだけ（opacityは
  レイアウトに影響しない）という控えめな実装にした。

## v3.14.0：プロフィールカード拡張・更新中表示の常設化・通常ページでのWASDQ・Gキーでメディア欄へ

- **プロフィールカードにウェブサイト・登録日・フォロー数・「〇〇さんにフォロー
  されています」を追加**：これまでアバター＋表示名＋ハンドル名＋自己紹介の
  みだったが、実際のプロフィールページに実機接続して`data-testid="UserUrl"`
  / `"UserJoinDate"`と、`/username/following`・`/username/verified_followers`
  へのリンクのhrefパターンを確認した上で実装（前回の検索欄の件を踏まえ、
  今回は必ず実物のDOMで検証してから実装した）。「フォローされています」の
  行だけはdata-testidが無いため、本文一覧の外側にある最小の要素をテキスト
  一致で探すヒューリスティックにした。**実機検証中に見つけたバグ**：
  `UserUrl`要素自体が`<a>`そのもので、子に`<a>`が入れ子になっているとは
  限らない。`querySelector('a')`では自分自身にマッチせず、href が常に空に
  なるバグがあったため、要素自体が`<a>`かどうかを先に判定するよう修正した。
- **更新中インジケータを常にスクロール位置に関係なく見える場所に移動**：
  v3.11.0ではグリッド最下部の読み込み中表示を流用していたが、更新中は
  まだ上の方を見ていることが多く気付きにくいという指摘があったため、
  sticky なツールバー側に専用の表示領域を追加した。
- **画像のみ表示OFFの通常タイムライン・単体ツイートのパーマリンクページでも
  WASDQ操作が使えるように**：自前グリッドを使っていないページでも、
  W/Sで記事を選択（青い枠線で表示）、Qで画像を拡大／閉じる、A/Dで複数画像
  ツイートの画像を切り替えられるようにした。拡大表示自体は既存のグリッド用
  オーバーレイの状態機械をそのまま流用しており、新規に実装したのは「どの
  記事が選択中か」を管理する部分と、その記事から画像を抜き出す処理のみ。
- **プロフィールページでGキーを押すとそのアカウントのメディア欄へ直接移動**
  するショートカットを追加。Xの「gに続けて別のキー」というプレフィックス
  ショートカットと衝突しないよう、処理後に`stopImmediatePropagation()`で
  Xの本来のハンドラに渡らないようにしている。

## v3.13.0：検索欄を復活（リンク方式）・アカウントタブの色分け強化

v3.12.0で「検索欄が中途半端」という指摘を「検索機能ごと不要」と誤読して
撤去してしまったが、実際の指摘は「検索欄をクリックしても、本来出るはずの
過去の検索履歴のような候補が出ず、真っ黒（＝何も無い状態）になる」という
話だった。自作の`<input>`はそもそもサジェスト機能を持たない偽物だったため
これは当然の挙動であり、直すべきは「機能する検索欄を作る」ことではなく、
最初から偽の入力欄を置かないことだった。

- **検索を「単なるリンク」として復活**：いいね/ブックマークと全く同じ
  パターン（`<a href="https://x.com/explore">検索</a>`）にした。本物の
  検索候補・オートコンプリートはX本体の`/explore`ページで正常に機能する。
- **アカウント固有タブ（ポスト/返信/リポスト等）の色分けを強化**：
  v3.10.0で追加した区切り線＋枠線だけでは「いいね/ブックマークと同じに
  見える」という指摘が繰り返し来ていた。枠線オンリーではなく、Xの
  ブランドカラー（青系）で塗りつぶしたピル型ボタンに変更し、区切り線の
  横に「このアカウント:」という見出しラベルも追加。これで色・境界線・
  ラベルの3点で共通ボタンと明確に区別できるようにした。

## v3.12.0：検索撤去・Qキー フォーカス同期バグ修正・プロフィールカード拡張

- **使い物にならない自作検索ボックスをツールバーから撤去**：サジェスト機能も
  無く、ただの`<input>`+送信フォームで検索URLに飛ぶだけだった。Xの標準検索は
  そもそも隠していないので不要と判断し、`content.js`（ツールバーの
  `innerHTML`構築と`searchForm`イベントリスナー）と`content.css`
  （`.xmr-tb-search`関連ルール）から完全に削除した。
- **Qキーで拡大表示を閉じた時のフォーカス同期バグを修正**：複数画像ツイートの
  拡大表示でA/Dキーを使って画像を送った場合、内部的には`Grid.viewIndex`
  しか更新しておらず`Grid.selIndex`（グリッド一覧側の選択位置）が更新
  されていなかった。そのためQで一覧に戻ると、選択ハイライトが送る前の
  古い画像の位置に残ったままになっていた。`closeOverlayLevel()`で、
  `Grid.viewList[Grid.viewIndex]`から`Grid.entries.indexOf()`により実際の
  インデックスを逆引きし、`Grid.selIndex`を同期してから`paintSelection()`を
  呼ぶように修正。
- **プロフィールのメディア欄カードに表示名と自己紹介文を追加**：v3.10で
  追加したアバター＋ハンドル名だけの`.xmr-profile-card`に、表示名
  （`data-testid="UserName"`、無ければ祖先のtextContentから`@handle`部分を
  除くヒューリスティックでフォールバック）と自己紹介文
  （`data-testid="UserDescription"`）を追加。`.xmr-profile-card-text`で
  ハンドル名の上に縦積み表示し、アバターも36pxに拡大した。

## v3.11.0：更新中インジケータ・既読トラッキング・複数画像A/D送りバグ修正

- **ホーム更新中であることが視覚的にわかるインジケータを追加**：更新ボタン
  または本物のホームアイコンで更新処理が走っている間、`Grid.loadingEl`に
  「更新中…」を表示するようにした。
- **コールドロード直後のスクロール位置ズレへの耐性を強化**：Shift+Enterで
  新しいタブに開いた直後など、`translateY`のリトライチェックが8回×250ms
  （最大2秒）では足りず途中の位置から表示されてしまうケースがあったため、
  16回×300ms（最大5秒）に増やした。
- **既読（見た目通過済み）トラッキング機能を新規実装**：`localStorage`に
  見たツイートIDを保存し（`CONFIG.seenStorageKey`、上限`CONFIG.seenCap`件で
  古い方から間引く）、`IntersectionObserver`(`Grid.seenObserver`,
  root=shell, threshold 0.5)でタイルが実際に画面を通過したら既読にする。
  既読タイルは帯の文字がうっすら青くなる(`.xmr-tile-seen`)。
- **複数画像ツイートでA/D送りが端で止まる不具合を修正**：単一画像ツイートは
  端まで行くと自然に次のツイートの画像に移れていたが、複数画像ツイートの
  サブグリッド／拡大表示では端で止まって次に進めなかった。新規関数
  `moveToAdjacentEntry()`を追加し、`step()`からサブグリッド／拡大表示の
  境界到達時に呼び出すことで、次(前)の写真系ツイートへシームレスに
  乗り換えられるようにした。

## v3.10.1：更新処理の作り直し（見た目のちらつきを解消）

「画像のみ表示ONのままホーム更新」を、①一旦グリッドを解除→②本物のページで
新着取り込み→③グリッドを作り直す、という手順で実装していたが、②の間だけ
本物のタイムラインが一瞬見えてしまい、「設定はOFFにしていないのに表示が
勝手に普通に戻る」ように見える不具合になっていた、との指摘を受けた。

**グリッドの見た目(shell/toolbar)は一度も消さず、中身（タイル）だけを
空にして裏のリストから先頭からもう一度拾い直す**方式に変更した
（`resetGridEntries()`）。新着取り込みのためのクリック自体は、opacity:0で
隠れている本物の要素に対して行っても問題なく効く（合成clickはヒットテストを
経由しないため見た目の状態に依存しない）ので、グリッドを解除する必要は
そもそも無かった。

## v3.10（起床後フィードバックへの対応・第3弾）

- **本物の「ホーム」ナビアイコンを押しても何も起きない不具合を修正**：
  グリッド表示中でも裏の本物のページでは「一番上へ移動＋新着取り込み」が
  起きているはずだが、グリッドは静的に組み立てた見た目のままなので、
  結果が画面に一切反映されず「押しても何も起きない」ように見えていた。
  ホームでグリッド表示中に本物のホームアイコンがクリックされたら、
  更新ボタンと同じ処理（v3.9の`refreshHomeTimeline()`）に割り込ませるように
  した。多重起動防止のため`Grid.refreshing`フラグを追加。
- **ツールバーの共通ボタンとプロフィール固有タブが紛らわしい件を修正**：
  検索/いいね/ブックマーク/更新（全ページ共通）と、ポスト/返信/リポスト等
  （そのアカウント固有）の間に区切り線を入れ、後者は枠線だけの見た目にして
  視覚的に分けた。
- **プロフィールのメディア欄にアバター＋ハンドル名の簡易カードを追加**：
  バナー・自己紹介を全部隠すと「Xを見ている感じがしない」という指摘を受け、
  アバター画像とハンドル名(@xxx)だけを拾った小さなカードを追加した。
  固定表示にはせず、グリッドの一番上の要素として普通に流し込んでいるので、
  スクロールすればXの通常のプロフィールページ同様に上へ流れて消えていく。

## v3.9（起床後フィードバックへの対応・第2弾）

- **画像のみ表示ONのままホーム更新が効かない不具合を修正**：裏に隠したまま
  新着バナーやホームアイコンをクリックしても反映されないという報告を受け、
  ユーザーが実際に成功している手動の手順（①画像のみ表示を一旦OFFにして
  本物のページを可視化→②ホームで新着を取り込む→③ONに戻す）をそのまま
  自動化する方式に作り直した（`refreshHomeTimeline()`）。
- **帯のアバターをクリック→投稿者のアカウントページへ、帯の文字/グレー部分を
  クリック→そのツイートへ**（いずれも新しいタブで開く）。画像自体のクリックは
  従来通り拡大表示。
- **拡大表示（ビューア）に投稿者・本文・「Xで開く」ボタンを追加**：画像だけだと
  情報が少ないという指摘を受け、Xの画像ポスト表示に近い形（画像＋右に
  投稿者アバター・本文・元ツイートを開く大きめのボタン）にした。いいね/
  リプライ等の本物のボタンをそのまま埋め込むことも検討したが、Xの
  React管理下のボタンをDOM上で移動させると動作が不安定になるリスクが
  高いため見送り、「Xで開く」ボタン経由で本物のページの本物のボタンを
  使ってもらう方式にした。
- **メディア欄で他のプロフィールタブ（ポスト/返信/リポスト等）に移動できない
  不具合を修正**：ネイティブなタブ切替をまとめて隠す処理が、
  `/i/history`のブックマーク/いいねタブ（自前のツールバーと重複するので
  隠して正解）と、プロフィールのタブ切替（隠すと移動手段が無くなる）を
  区別していなかった。隠す前に「ブックマーク/いいね」以外のタブのリンクを
  抽出し、ツールバーに載せ替えるようにした。

## v3.8（起床後フィードバックへの対応）

- **「メディア欄から離れて戻ると先頭から読み込まれない」の根本原因を実機で特定**：
  `window.scrollY`を0にリセットした直後でも、実際にDOMに描画されている
  セル（`cellInnerDiv`）の`translateY`が4000px超のまま（＝Xの仮想リストが
  スクロールリセットに追いついていない）という状態を実機で確認できました。
  スクロール位置(`window.scrollY`)を見るだけでは不十分で、**実際のセルの
  `translateY`を見ながら、先頭付近（400px未満）に落ち着くまで最大2秒
  リトライする**方式に変更しました。
  - 検証環境の制約: 確認に使ったタブがバックグラウンド状態（画面ロック等）
    だったため、Xの仮想リスト自体の更新が強くスロットリングされ、この
    リトライでも解決しないケースを観測しました。ただしこれは検証環境固有の
    問題で、通常の（フォアグラウンドの）利用時はもっと短時間で仮想リストが
    追いつくはずです。実際の使用時に直っているか確認をお願いします。
- **ホームの「更新」ボタンが効かない不具合を修正**：単純なページリロードだと、
  Xがサーバーから返す初期タイムラインが「N件の新着」バナーが指す最新状態より
  古いことがあり、「更新しても変わらない」状態になっていました。ホームでは
  実際に新着を取り込む操作（新着バナーを探してクリック、無ければホーム
  アイコンをクリック＝既にホームにいる状態でもう一度押すと最新に更新される
  Xの標準挙動）を裏で行ってからグリッドを作り直すようにしました
  （`refreshHomeTimeline()`）。
- **画像タイルの下に帯（投稿者・本文）を追加**：app.htmlの「画像＋下の帯」
  デザインに合わせ、写真タイルにも画像の下に投稿者アバター・本文を表示する
  帯を追加しました。本文が無い場合は帯はグレーのまま（高さは確保）。
  ホーム・メディア・いいね・ブックマークの全モードが同じ`makeTile()`を
  共有しているため、この変更は全モードに自動的に反映されます。

## v3.7（就寝中の予約タスクで対応）

ユーザーが寝ている間に、残っていた課題を優先順位順に確認・対応しました。
**実行環境の制約**: このタブはバックグラウンド（`document.hidden === true`）で
動いており、Chromiumはバックグラウンドタブのネットワーク/JS実行を強く
スロットリングするため、スクロールによる大量読み込みを伴う検証（グリッドの
先頭読み込み・複数画像ツイートの発見等）は途中で頭打ちになり、十分な確認が
できませんでした。以下、結果です。

- **左ナビが文字なしなのに幅を取っている件 → 撤回して決着**：実機で深掘りした結果、
  ラベルspanを隠しても`<a>`要素自体がflex-grow:1で259px前後まで広がっており、
  それを個別に縮めても（86px程度まで縮む）、`<a>`とheaderの間にある複数階層の
  無名ラッパーdiv（自動生成クラス名）がそれぞれ古い幅を独立して保持していて、
  ナビ全体の幅（335px）は結局縮まらないことが分かりました。特定のセレクタに
  依存しない汎用的な縮小方法が見つからず、これ以上追いかけると脆いコードに
  なるため、「どちらかに決着させろ」の指示通り**ナビ圧縮自体を撤回**し、
  Xの標準ナビ表示に戻しました（`compactNav()`を削除、`.xmr-nav-shrink`等の
  CSSも削除）。
- **ポスト欄が細すぎる件 → 実機確認では既に解消済み**：直接ステータスページに
  遷移して`primaryColumn`の幅を実測したところ、`600px`（Xの素の既定値）ちょうどで、
  `.xmr-wide-main`クラスも付与されていませんでした。v3.5.0時点の撤回が正しく
  効いていることを確認できました。
- **コールドロード時にサイドバーの置き換えボタン／ホームのトグルボタンが
  出ないことがある件 → 根本原因を特定して修正**：`manifest.json`は
  `run_at:"document_start"`のため、`document.body`がまだ存在しないタイミングで
  content scriptが動き出すことがあります。初回ロードが直接`/home`だった場合、
  `ensureHomeToggle()`内の`document.body.appendChild(btn)`が`document.body`が
  `null`のまま呼ばれて例外を投げ、その例外が`onUrlChange(onNavigate)`の初回
  同期呼び出しを通じて外に伝播し、**それより後ろに書かれていた
  `bodyObserver`のセットアップコードごと丸ごと実行されなくなっていた**ことが
  コードを読んで判明しました（以後の自動リトライ手段が最初から存在しなくなる）。
  `ensureHomeToggle()`の先頭に`document.body`の存在チェックを追加し、さらに
  `onUrlChange`側でも`cb()`呼び出し全体を`try/catch`で包んで、どんな例外が
  起きても以降の初期化が止まらないようにしました。
- **Shift+Enterが新しいウィンドウで開く件**：v3.6.1で`setTimeout`遅延により
  修正済み（このセッションでは環境の制約でクリックイベント込みの再現テストは
  できず、コードレビューでロジックが妥当であることのみ確認）。
- **メディア欄／いいね欄が先頭からではなく途中から読み込まれる件**：
  v3.5.1の`window.scrollTo(0,0)`修正はコード上正しく残っていることを確認
  しましたが、上記の環境制約（バックグラウンドタブでの読み込みスロットリング）
  により、スクロール→離脱→復帰という実際のシナリオを最後まで再現しての
  確認はできませんでした。**ユーザーの目で再確認をお願いします**。
- **複数画像ツイートを旧Twitter風の2x2ウィンドウ表示にする件（新規機能） →
  未実装、投資判断が必要**：同様の環境制約により、ホーム/いいね欄で複数画像を
  持つツイートをスクロールで見つけて実際のDOM構造（`tweetPhoto`を囲む
  コンテナのdisplay/grid-template-columns等）を調べることができませんでした。
  この機能はサイト全体のツイート表示に影響する（グリッド専用ページより
  影響範囲が大きい）ため、DOM構造を確認しないまま実装するのはこのセッションで
  繰り返した回帰と同じリスクがあると判断し、見送りました。次回、実機で
  複数画像ツイートを一緒に確認してから着手するのが安全です。

## v6（v3.0.0）：グリッドの土台を作り直した

これまでのグリッドは「primaryColumnの幅をXのCSSに逆らって広げる」方式で、
Xの内部ラッパー（ズーム倍率で微妙にpx数が変わる）と噛み合わせを取り続ける必要が
あり、そのたびに壊れていました（125%ズームで崩れる、列の幅が2136pxまで暴走する、等）。

**app.htmlが同じ操作をしても壊れないのは、Xの既存レイアウトと一切干渉しない
独立ページだから**という指摘の通りだったので、グリッドの実装も同じ考え方に
作り直しました：

- グリッドは`position:fixed`で「ナビの右端からビューポート右端まで」を覆う、
  Xのレイアウト計算に一切参加しない独立オーバーレイに変更。
- ナビの右端の位置・グリッドの開始位置(Y座標)は、CSSで指定するのではなく
  **画面から実測**して決める（ズーム倍率が変わっても実測値なので追従する）。
- primaryColumnの幅を無理に広げる処理はグリッド表示中は使わなくなった。
- 列数の初期値を3→5に変更（app.htmlに合わせた。多い方が「デカすぎる」問題も
  緩和されるはず）。グリッド上部の「列数」欄は常に画面上部に固定表示されるようにした。

## v5で直した点（実機フィードバックより）

- **Q/Spaceのやり直し**：v4で「Qは常に閉じる」にしすぎて、グリッド一覧からの最初の
  オープン操作までQで開けなくなっていた。正しくは「グリッド一覧からはQで開く（1枚なら
  ビューア、複数ならサブグリッドへ）。サブグリッド／ビューアに入った後はQは常に閉じる。
  サブグリッドで1枚選んで開く操作だけSpace」。この形に戻した。
- **右サイドバーのウィジェットが消えない不具合の再修正**：trends部分は`<section>`タグで
  囲まれていることが分かったので、それを直接の境界にするよう変更（兄弟要素を数える
  ヒューリスティックはやめた）。
- **ガクつきの再修正**：確定済みセルにマーキングして次回以降の全走査から即スキップする
  ようにした。
- **グリッドの表示幅がprimaryColumnの上限(1000px)まで広がっていなかった不具合を修正**：
  primaryColumnの内側にもう一段600px固定のラッパー(`<section>`込み)があり、そちらの
  上限は解除できていなかった。sourceRootからprimaryColumnまでの経路を全部たどって解除。
- **左ナビが項目によって縮まっていなかった不具合を修正**（「クリエイタースタジオ」等
  data-testidの無い項目が漏れていた）：testidのホワイトリスト方式をやめ、ナビ内の
  クリック可能な項目を全部まとめてラベル非表示にするよう変更。
- **グリッド表示中は右サイドバーを丸ごと非表示に**：検索・いいねの履歴・閲覧履歴・
  ブックマークへの導線はグリッド上部のツールバーに移設し、空いた幅を画像グリッドに
  使うようにした（検索欄は自前の簡易フォームで、Enterで検索結果ページへ遷移する方式。
  Xの検索欄そのものは複雑な内部状態を持つため移動させず、別の入力欄を用意した）。

## v2で設計を作り直した理由

v1は「Xが仮想スクロールで管理しているDOMの位置(transform)をJS側から強制的に
上書きする」方式でしたが、実機で試してもらったところ以下の不具合が出ました：

- グリッドに大きな空白の隙間ができる
- 少しスクロールすると、最初にロードされた分より下に進めなくなる（Xの追加読み込みが動かない）
- 左ナビを細くする処理が原因不明の表示崩れを起こした

原因は、Xの内部実装（Reactが仮想リストの位置を再計算するたび上書きし直す
「せめぎ合い」）と、Xが実際のスクロール位置をもとに「次を読み込むタイミング」
を判断しているロジックの両方を、位置の書き換えで壊してしまっていたことでした。

**v2では方針を変え、Xの本物のリストには一切スタイルを当てず、裏で普段どおり
動かしたまま透明化するだけにしました。** そこから画像・テキストを抽出して、
まったく別物の自前グリッド（普通の `<img>` タグを並べただけのシンプルなDOM）
として表示します。グリッドを下までスクロールすると、裏の透明なリストを
JS側で自動スクロールさせてXの通常の追加読み込みを誘発し、増えた分だけ
グリッドに継ぎ足します。Xの内部実装への依存が「スクロールすると増える」
という一番外側の挙動だけになるので、v1よりだいぶ壊れにくいはずです。

また、左ナビを細くする機能（v1機能7）は不具合の原因になっていたのと、
「2列表示をしないなら別に消さなくていい」とのことだったので**撤回**しました。
今は左ナビ・右サイドバーの幅はXの標準のままです（右サイドバーの中身の
一部だけ削除・置き換えしています）。

## v3で直した点（実機フィードバックより）

- **左に無駄な余白ができる／125%ズームで右端が切れる**：原因は
  `header[role="banner"]`（左ナビ）が `flex-grow:1` で実際のアイコン幅より
  大きく確保され、中央カラムを右に押しやっていたことでした（実機のDOM調査で確認）。
  v2の「中央カラムを固定900pxに広げる」対処療法をやめて、**ナビ側の
  `flex-grow` を止める**方式に変更しました。固定pxを使わないので、画面幅や
  ズーム倍率が変わっても自動的に追従します。
- **いいね欄で画像付きのはずのツイートが真っ黒な文字カードになる**：実機のDOM調査で、
  Xは投稿の本文・アバターなどの骨組みを先に描画し、画像本体
  (`data-testid="tweetPhoto"`) は数秒遅れて追加されるケースがあることを確認しました。
  旧実装は「見た瞬間に画像が無ければテキストのみと確定」していたため、これを
  文字カードとして固定してしまっていました。**v3では画像が見つからない場合すぐに
  確定せず、最大4秒待って画像が現れないか再チェックしてから確定する**ように変更しました。
- **スクロール時の軽いガクつき**：DOM変化のたびに毎回リストを走査していたのを、
  `requestAnimationFrame`でまとめて処理するよう変更しました。
- メディア欄の「上の方の投稿が出ない」現象は、初期マウント時の取りこぼしが原因の
  可能性があるため、コンテナの取得を少し待ってからやり直す対策を入れました
  （実機でこの現象を再現できていないため、直っているか要確認）。

## v4で直した点（実機フィードバックより）

- **キー操作をQ(閉じる専用)とSpace(選択して開く)に分離**：複数画像のときQを2回押すと
  開いてしまい「閉じたいのに開く」事故が起きるとの指摘を受け、Qは常に1階層戻る、
  Spaceで選択中のものを開く、に統一しました。
- **グリッドのタイル形状を正方形→縦長(3:4)に変更**：x-taste-digestのapp.htmlと同じ
  `aspect-ratio:3/4` を採用し、画像が見切れる量を減らしました。
- **列数を画面上の数字入力で変更できるように**：グリッド左上に「列数」入力欄を追加。
  app.htmlの `#cols` と同じ仕組みです。
- **右サイドバーのウィジェット除去ロジックを修正**：ページによって内部構造が少し違い、
  「いま」を見つけようウィジェットの一部しか消せていなかった（見出しだけ消えて
  トレンド一覧が残る、置き換えボタンが画面外にずれる、等）不具合を修正しました。
- **左ナビをさらにコンパクトに**：ラベル文字を隠してアイコンだけにしました
  （項目自体は残したまま、文字を消して幅を縮める方式）。
- **中央カラムをさらに拡張**：primaryColumnの外側にあった幅クランプ用ラッパーを解除し、
  上限を600px→1000pxに引き上げました。

## 実装した機能

| # | 機能 | 状態 |
|---|------|------|
| 1 | `/user/media` を開くと自動で `?filter=photo` にリダイレクト | 実装済み |
| 2 | メディアタブを密なグリッド表示に再構成（3列、余白詰め） | v2で再実装・**要検証** |
| 3 | 複数画像ツイート：グリッドは代表1枚→クリックで2x2→クリックで拡大 | 実装済み |
| 4 | ホバー中のポストを1キーで「興味がない」に（デフォルト: `x` キー） | 実装済み |
| 5 | 右サイドバーの「プレミアム勧誘」「おすすめユーザー」を削除 | 実装済み |
| 6 | 「いま」を見つけよう → 「いいねの履歴」「閲覧履歴」ボタンに置き換え | 実装済み（動作確認済み） |
| 7 | ~~左ナビをアイコンのみに細くする~~ | **撤回**（不具合の原因になったため） |
| 8 | 中央カラムを拡張 | 実装済み |
| 9 | グリッド内 WASD 移動 / Q 拡大・戻る / Esc 閉じる（x-taste-digestと同じ操作感） | 実装済み |
| 10 | `x.com/i/history/likes`（いいねの履歴）も3列グリッド化。画像ツイートは①〜③と同じ扱い、テキストのみのツイートは小さな文字カードとして同じグリッドに混在表示 | v2で再実装・**要検証** |
| - | タイムラインの2列表示（ツイートを横に2つ並べる） | **見送り**（理由は下） |

## キー操作（メディアグリッド／いいねグリッド表示中）

- `W/A/S/D` または矢印キー：選択移動（`A/D`＝前後、`W/S`＝1行上下）
- `Q`：選択中のサムネを拡大 ／ 拡大表示・サブグリッドを閉じる ／ 文字カードならそのツイートへ
- `Esc`：1階層戻る
- クリックでも同様に操作可能

## 「興味がない」ワンキー操作について

ポストにマウスを乗せた状態で `x` キーを押すと、「…」メニューを自動で開いて
「このポストに興味がない」を自動クリックします。キーを変更したい場合は
`content.js` 冒頭の `CONFIG.notInterestedKey` を書き換えてください
（`j`/`k`/`l`/`r`/`t`/`m`/`o`/`.`/`/` はXの標準ショートカットと衝突するため避けてください）。

## いいね欄でのテキストのみツイートの扱い（提案・採用した案）

いいね欄には画像を持たないツイート（テキストだけ、動画のみ、引用RTなど）も混ざるため、
以下のように決めて実装しました：

- **グリッドから除外せず、画像ツイートと同じ3列グリッドの中に、時系列順のまま
  「小さな文字カード」として混在させる**方式を採用しました。
  - 案A（グリッドから除外して隠す）は「いいね欄を全部俯瞰したい」という密度目的に反する
  - 案B（全幅の帯としてグリッドを分断する）は3列の一覧性が崩れる
  - 採用したC案（同じマス目に文字カードとして収める）が、密度と網羅性を両立できると判断
- 文字カードにはアバター（小）・投稿者名・本文（6行までで省略）だけを表示し、
  いいねボタン等のアクションバーは非表示にしています。
- WASD移動では文字カードも通常のマスとして選択できますが、`Q`（拡大）を押すと
  画像ビューアではなく、そのツイートへ直接ジャンプします（画像が無いので拡大表示のしようがないため）。
  マウスでクリックした場合も同様です。
- 画像ビューア中に `A/D`（前後移動）で送ると、文字カードは読み飛ばして
  画像を持つツイートだけを順番に表示します（文字カードのところで画像ビューアが
  空白になるのを防ぐため）。

この案で一旦作ってあります。実際に見てみて「やっぱり隠したい」「文字カードの
情報量を増やしたい／減らしたい」等あれば調整します。

## 既知の制約・要検証ポイント（v2）

- **画面上部のズレ**：自前グリッドは `position: sticky; top: 53px` で画面に固定していますが、
  この `53px` はXのページ内ヘッダー（メディア/画像の切替タブなど）のだいたいの高さを
  決め打ちしています。ページによってヘッダーの高さが違う場合、グリッドの上端が
  ヘッダーと少し重なる／隙間が空く可能性があります（`content.js` の
  `CONFIG.stickyTopOffset` で調整可能）。
- **初回表示の待ち時間**：グリッドを開いた直後、裏のリストを自動スクロールして
  30件たまるまで軽く読み込みが走ります（`CONFIG.initialFillCount`）。回線やXの
  応答速度によっては1〜2秒ほど埋まるまで時間がかかることがあります。
- **画像URLの取得**は `data-testid="tweetPhoto"` を目印にしているため、動画のみの
  ツイートはメディアタブのグリッド化の対象外です（`filter=photo` 想定のため許容範囲のはず）。
  いいね欄では動画のみのツイートも文字カード扱いになります。
- **タイムラインの「ツイートを2列に並べる」機能は見送りました。** ホームタイムラインは
  各ツイートの高さがバラバラな上、Xの仮想スクロール実装への依存度がメディアグリッドより
  ずっと高くなるため、v1で起きたのと同種の不具合が起きるリスクが高いと判断しました。
- 実際のBraveブラウザでv2の動作確認はまだできていません。読み込んで崩れる/効かない箇所が
  あれば、今回のように画面と状況を教えてもらえると直しやすいです。

## ファイル構成

- `manifest.json` — 拡張機能の定義（x.com / twitter.com にContent Scriptを注入するだけ）
- `content.js` — 全ロジック
- `content.css` — 静的なスタイル
